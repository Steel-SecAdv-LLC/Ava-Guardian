# AMA-Cryptography pinned development dependencies
# Generated: 2026-07-30
# Python: 3.11.15
# Platform: linux (x86_64)
#
# Reproduce with: pip install -r requirements-lock.txt
#
# This file is the complete closure of the pinned toolchain: a clean-venv
# `pip install -r requirements-lock.txt -r requirements-dev.txt` followed by
# `pip freeze --all` must resolve to exactly the pins below (plus pip itself).
# CI runs `pip-audit --strict` against this file as the audit-time dependency
# closure, so a transitive that is installed but unlisted is a hole in the
# audit.  The curated sections are kept (a raw freeze loses the comments);
# update by re-running the clean-venv freeze and folding the diff in.
#
# NOTE: The library itself has ZERO runtime dependencies.
# These are development/testing/CI dependencies only.
#
# v3.2.0 — `safety` and its transitive chain (nltk, joblib, dparse,
# ruamel.yaml, tenacity, tomlkit, typer, safety-schemas, plus their
# pydantic / httpx / Authlib sub-transitives) were removed: `safety`
# was a dev-deps inventory entry that no CI workflow ever invoked
# (vulnerability scanning is already covered by `pip-audit --strict`
# in both .github/workflows/ci-build-test.yml::security and
# .github/workflows/security.yml::security-audit), so it was pure
# transitive surface area carrying two CVEs with no upstream fix
# versions (joblib PYSEC-2024-277, nltk PYSEC-2026-97).  Removing
# `safety` eliminates the chain entirely — INVARIANT-1 zero-runtime-
# dep posture and INVARIANT-14 CVE-ignore-list hygiene both improve.

# --- Core test framework ---
pytest==9.1.1
pytest-cov==7.1.0
hypothesis==6.161.8
coverage==7.15.2

# --- Linting & formatting ---
black==26.5.1
mypy==2.3.0
mypy_extensions==1.1.0
ruff==0.16.0

# --- Security scanning ---
bandit==1.9.4
# pip-audit (installed separately in CI) provides dependency vulnerability
# scanning via the OSV / PyPI advisory feeds — same coverage `safety` did,
# without `safety`'s transitive chain (nltk + joblib).

# --- Scientific (monitoring extras) ---
numpy==2.4.4

# --- Build toolchain ---
# Emitted by the clean-venv freeze (requirements-dev.txt pins their floors);
# in the pip-audit scope deliberately — build tooling is supply chain too.
build==1.5.0
Cython==3.2.9
pyproject_hooks==1.2.0
setuptools==83.0.0
wheel==0.47.0

# --- Transitive dependencies ---
ast_serialize==0.6.0
click==8.4.2
iniconfig==2.3.0
librt==0.13.0
markdown-it-py==4.2.0
mdurl==0.1.2
packaging==26.2
pathspec==1.1.1
platformdirs==4.11.0
pluggy==1.6.0
# Pygments 2.20.0 includes the CVE-2026-4539 AdlLexer ReDoS fix.
# Pygments remains a transitive dev dependency only; it is not used at runtime.
Pygments==2.20.0
PyYAML==6.0.3
pytokens==0.4.1
rich==15.0.0
sortedcontainers==2.4.0
stevedore==5.8.0
types-PyYAML==6.0.12.20260724
types-setuptools==84.0.0.20260812
typing_extensions==4.16.0
