# Production Dependencies
# Install with: pip install -r requirements.txt

# Core cryptographic library (required)
cryptography>=41.0.0

# Quantum-resistant cryptography (recommended for production)
# The PyPI package is 'oqs' (not 'liboqs-python')
# Provides ML-DSA (Dilithium) and ML-KEM (Kyber) implementations
oqs>=0.10.0,<0.11.0

# NOTE: The 'oqs' package wraps the liboqs C library for quantum-resistant crypto.
# If installation fails, ensure liboqs is installed on your system:
#   - Ubuntu: sudo apt-get install liboqs-dev
#   - macOS: brew install liboqs
#   - Or build from source: https://github.com/open-quantum-safe/liboqs
# Alternative (less maintained): pqcrypto>=0.1.3,<0.2.0

# Secure memory operations (optional but recommended)
# Provides memory locking (mlock) and secure zeroing via libsodium
# Install for enhanced memory protection in high-security environments
pynacl>=1.5.0

# 3R Monitoring dependencies
# NumPy: 1.24.x is last version supporting Python 3.8
numpy>=1.24.0,<2.0.0; python_version < "3.9"
numpy>=1.24.0; python_version >= "3.9"

# SciPy: 1.10.x is last version supporting Python 3.8, 1.11+ requires Python 3.9+
scipy>=1.10.0,<1.11.0; python_version < "3.9"
scipy>=1.11.0; python_version >= "3.9"
