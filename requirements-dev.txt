# Development and Testing Dependencies for AMA Cryptography
# Install with: pip install -r requirements-dev.txt
#
# ALL cryptographic operations provided natively by the C library (ZERO external deps).
# Native C backend: Ed25519, AES-256-GCM, HKDF, ML-DSA-65, Kyber-1024, SPHINCS+.
# Build with: cmake -B build -DAMA_USE_NATIVE_PQC=ON && cmake --build build

# Build dependencies (required for Cython extensions).
# The project floor is Python 3.10 (requires-python >= 3.10), so these are
# unconditional — no version-gated fallback rows for EOL interpreters.
Cython>=3.2.8

# NumPy for the optional math / monitoring extensions.
numpy>=1.24.0

# SciPy for the monitoring extension (1.11+ supports the 3.10+ floor).

# Testing
pytest>=9.1.1
pytest-cov>=4.0.0
hypothesis>=6.161.8

# Code Quality
black>=26.5.1
ruff>=0.16.0
mypy>=2.3.0
# PyYAML powers tools/check_workflow_commands.py (INVARIANT-25), which parses
# .github/workflows/** structurally rather than by regex.  types-PyYAML keeps
# that tool and its tests inside `mypy --strict` instead of behind an override.
PyYAML>=6.0.3
types-PyYAML>=6.0.12.20260724
# Same case, same reason: setup.py is inside `mypy --strict` and setuptools
# ships no `py.typed`, so without this stub the type-check fails with 5 errors.
# It was added to pyproject's [dev] extra and to requirements-lock.txt and NOT
# here, so this file — which README.md presents as the equivalent alternative
# to `pip install -e ".[dev]"` — still could not run `make lint`.
types-setuptools>=84.0.0.20260812

# Security Scanning
bandit>=1.7.0
# pip-audit (installed in CI) provides dependency vulnerability scanning via OSV.

# Benchmarking (optional — benchmarks/benchmark_suite.py uses psutil when available)
# psutil>=5.9.0

# Build and packaging
setuptools>=83.0.0
wheel>=0.47.0
build>=1.5.0
