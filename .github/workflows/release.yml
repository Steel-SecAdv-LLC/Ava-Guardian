# Copyright (C) 2025-2026 Steel Security Advisors LLC
# SPDX-License-Identifier: Apache-2.0
name: Release — Build, Sign, and Publish

# Tagged-release pipeline for ama-cryptography (audit Issue 1).
#
# INVARIANT-11 declares the SBOM as the release gate.  Prior to this
# workflow, `git tag --list` was empty and no `release.yml` existed;
# the `tags: ['v*']` triggers on acvp_validation.yml and security.yml
# would have fired once a tag was pushed, but nothing actually built
# and published a wheel.  This workflow closes the gap.
#
# The pipeline runs ONLY on `v*` tag pushes (and manual workflow_dispatch
# for dry-runs that don't publish).  Pull requests, branch pushes, and
# scheduled runs DO NOT trigger this workflow — release-grade signing
# keys (sigstore identity, PyPI Trusted Publishing) are only valid
# when the tag-protection ruleset is exercised, and a misconfigured
# trigger here is the easiest way to leak a published artefact.
#
# Stages, in order:
#
#   1. preflight       — re-derive the package version from the tag,
#                        regenerate the C-library SBOM, sanity check
#                        that the version-consistency script passes.
#                        FAILS the release if anything drifted.
#
#   2. build-wheels    — cibuildwheel matrix across Linux/macOS/Windows
#                        and Python 3.10–3.14.  Each wheel ships the
#                        Ed25519-signed _integrity_signature.py
#                        artefact via setup.py's AMA_BUILD_PIPELINE
#                        hook.
#
#   3. build-sdist     — single sdist (tarball) for completeness.
#
#   4. provenance      — slsa-framework/slsa-github-generator builds
#                        SLSA v1 provenance attestations over every
#                        wheel + the sdist.  The attestation lands in
#                        a sibling artefact and is uploaded to PyPI
#                        alongside the wheels via the trusted publisher.
#
#   5. sign            — sigstore-python signs each wheel + the sdist
#                        with the keyless OIDC-bound identity that
#                        GitHub Actions exposes.  Signatures live in
#                        the artefact alongside the binary they sign.
#
#   6. publish-pypi    — PyPI Trusted Publishing (no long-lived token).
#                        Requires the PyPI project's "trusted publisher"
#                        rule to permit
#                          repo = Steel-SecAdv-LLC/AMA-Cryptography
#                          workflow = release.yml
#                          environment = pypi
#                        publish-pypi carries `environment: pypi`, but
#                        referencing an environment does NOT by itself require
#                        approval: the required-reviewer protection must be
#                        configured on Settings -> Environments (same as the
#                        `release` environment note on github-release below).
#                        Until it is, the block documents the intended gate
#                        rather than enforcing it (audit M10).
#
#   7. github-release  — create the GitHub Release with the changelog,
#                        attach the wheels + sdist + provenance +
#                        signatures.
#
# Operator runbook:
#
#   1. Dry run FIRST, from the branch you intend to tag:
#
#        Actions -> "Release — Build, Sign, and Publish" -> Run workflow
#        (dry_run defaults to true; publish-pypi and github-release are
#        gated on a tag push and cannot fire from a dispatch.)
#
#      This is not optional ceremony.  Everything below stage 1 runs ONLY
#      on a tag push, so nothing else in CI ever executes it.  Five
#      independent release blockers reached production that way — a
#      cibuildwheel pin to a nonexistent commit, a retired `macos-13`
#      runner label, a `python -c` payload broken by YAML folding,
#      POSIX quoting handed to cmd.exe, and a cibuildwheel 3->4 bump that
#      turned on delvewheel Windows repair by default (see
#      CIBW_REPAIR_WHEEL_COMMAND_WINDOWS below) — and releases shipped with
#      zero artefacts as a result.  The v3.2.0 and v3.3.0 releases still
#      carry no assets and, being immutable, cannot be repaired; v3.5.0's
#      first attempt failed the same way and was re-tagged onto the fix
#      commit, so the published release now carries all 54 artefacts.
#      tools/check_workflow_commands.py now catches the first four classes
#      statically on every PR; the delvewheel default is a behavioural
#      change in a dependency, not a command defect, so NO static check
#      sees it — a dry run is the only thing that exercises the real repair
#      step against the real matrix and runner fleet.
#
#      A recorded green dry run covers a COMMIT, not a branch, and this
#      file is not the only input that invalidates it.  The release path
#      the dry run exercises is: this workflow, `setup.py` (the
#      AMA_BUILD_PIPELINE post-build signer cibuildwheel invokes),
#      `ama_cryptography/_build_sign.py`, `tools/resign_wheel.py` (the
#      Linux post-repair re-signer), `tools/wheel_smoke_test.py`
#      (CIBW_TEST_COMMAND), and `tools/check_release_tag.py`.  If ANY of
#      those changed since the last green dispatch run, the dry run is
#      stale and must be re-run before tagging — this is a procedural
#      obligation (INVARIANT-25 records why no static checker can
#      discharge it), and it has been missed in the exact inverse
#      direction before: a re-run was once demanded "because release.yml
#      changed" when release.yml was byte-identical to the recorded run
#      and it was setup.py that had moved.
#
#   2. Tag only after the dry run is green.  The tag MUST be annotated and
#      signed — `-s`, not a bare `git tag` — and preflight now enforces it
#      (tools/check_release_tag.py).  This is not ceremony either: of the
#      eleven tags this repository carried before that gate existed, six were
#      lightweight (a mutable ref straight to a commit, with no object to sign)
#      and the other five were annotated but unsigned.  Every one of those
#      releases went out through a pipeline whose runbook said `git tag -s`.
#
#        $ git tag -s v5.0.0 -m "ama-cryptography 5.0.0"
#        $ git push origin v5.0.0
#
#      For GitHub to show the tag as Verified, the signing key must also be
#      registered on the account under Settings -> SSH and GPG keys with type
#      "Signing Key" — an authentication key of the same material does not
#      satisfy it.  preflight reports GitHub's verdict without gating on it,
#      since that setting is account state, not repository state.
#
# All of stages 1-7 then run automatically.  A failure in any stage halts
# the pipeline before the next; the published artefact is exactly the
# one that passed every gate.

on:
  push:
    tags: [ 'v*' ]
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Build but do not publish (dry-run smoke test).'
        type: boolean
        default: true

# Top-level permissions are minimal.  Each job below grants the
# additional permissions it needs: id-token for OIDC; contents: write on
# the provenance job (the slsa-github-generator reusable workflow's
# internal upload-assets job statically requires it) and on the
# github-release job (to publish the release + assets).
permissions:
  contents: read

# Avoid clobbering an in-flight release if a maintainer accidentally
# pushes a second tag while the first is still building.  Cancellation
# is OFF — we never cancel a release in progress; the second tag must
# wait.  The group is STATIC for exactly that reason: a per-ref group
# (the previous `release-${{ github.ref }}`) serialized only re-pushes
# of the SAME tag, while two different tags — the accident this block's
# own comment names — ran fully in parallel.
concurrency:
  group: release
  cancel-in-progress: false

jobs:
  preflight:
    name: Preflight (tag + version + SBOM coherence)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag_clean: ${{ steps.version.outputs.tag_clean }}
      is_prerelease: ${{ steps.version.outputs.is_prerelease }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.11"

      - name: Resolve tag → version (must match pyproject.toml)
        id: version
        run: |
          # When run via tag push, github.ref looks like refs/tags/vX.Y.Z.
          # When run via workflow_dispatch, github.ref is a branch — we
          # let the build proceed but mark it as a dry-run.  In either
          # case we anchor on pyproject.toml as the canonical version.
          tag="${GITHUB_REF##*/}"
          tag_clean="${tag#v}"
          pyproject_version=$(python -c "
          import re, sys
          from pathlib import Path
          m = re.search(r'^version\s*=\s*\"([^\"]+)\"',
                        Path('pyproject.toml').read_text(encoding='utf-8'),
                        re.MULTILINE)
          if m is None:
              sys.exit('pyproject.toml has no [project].version')
          print(m.group(1))
          ")
          # Classify the release channel from the CANONICAL pyproject version,
          # not from the tag string.  The tag must equal pyproject exactly
          # (the equality gate below), and pyproject is PEP 440 — where a
          # pre-release is `rcN`/`aN`/`bN` and a dev release is `.devN`, with
          # NO hyphen.  The GitHub-release job used to sniff the tag for the
          # SemVer spellings `-rc`/`-alpha`/`-beta`, which a PEP 440 tag such
          # as v5.0.0rc1 never contains, so an rc tag published as a full,
          # non-draft release.  Deriving the flag here, from the same string
          # the equality gate anchors on, closes that gap.  `.postN` is a
          # final release, not a pre-release, and must NOT match.
          is_prerelease=$(AMA_PYPROJECT_VERSION="$pyproject_version" python -c "
          import os, re
          v = os.environ['AMA_PYPROJECT_VERSION']
          # PEP 440 pre-release (aN/bN/rcN) or dev-release (.devN) segment.
          print('true' if re.search(r'(?:a|b|rc)[0-9]+|\.dev[0-9]+', v) else 'false')
          ")
          echo "Tag (raw):           $tag"
          echo "Tag (clean):         $tag_clean"
          echo "pyproject.toml:      $pyproject_version"
          echo "Pre-release channel: $is_prerelease"
          if [ "${{ github.event_name }}" = "push" ]; then
            if [ "$tag_clean" != "$pyproject_version" ]; then
              echo "::error::Tag $tag_clean does not match pyproject.toml version $pyproject_version."
              echo "         Bump pyproject.toml (and all the artifacts tools/check_version_consistency.py"
              echo "         tracks) and re-tag from the new commit."
              exit 1
            fi
          fi
          {
            echo "version=$pyproject_version"
            echo "tag_clean=$tag_clean"
            echo "is_prerelease=$is_prerelease"
          } >> "$GITHUB_OUTPUT"

      - name: Require an annotated, signed tag (INVARIANT-10)
        # Only meaningful on a tag push; a workflow_dispatch dry-run has no
        # tag to inspect and is explicitly a dry-run.
        if: github.event_name == 'push'
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF##*/}"
          # actions/checkout at its default depth fetches the COMMIT the tag
          # resolves to and writes a local refs/tags/<name> pointing at it.
          # That local ref is lightweight even when the pushed tag is
          # annotated, so the checker below would report a false lightweight
          # verdict on a correctly signed tag.  Re-fetch the tag ref itself,
          # with --force, so the annotated object is what gets inspected.
          git fetch --force --no-tags origin "refs/tags/${TAG}:refs/tags/${TAG}"
          python tools/check_release_tag.py "${TAG}"
          # The shape check above is fail-closed and needs no key material.
          # GitHub's own verified/unverified verdict is account-level state —
          # it turns on when the signing key is registered under
          # Settings -> SSH and GPG keys with type "Signing Key" — so it is
          # reported here for the operator to read rather than gated on.  A
          # release must not be blocked by a setting that lives outside the
          # repository, but it must not hide it either.
          echo "GitHub signature verdict for ${TAG}:"
          gh api "repos/${GITHUB_REPOSITORY}/git/refs/tags/${TAG}" \
            --jq '.object.sha' \
          | xargs -I{} gh api "repos/${GITHUB_REPOSITORY}/git/tags/{}" \
            --jq '"  verified=\(.verification.verified)  reason=\(.verification.reason)"' \
          || echo "  (unavailable — informational only, not a release blocker)"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Refuse an unanchored release from the canonical repository (H3)
        # The anchored/unanchored split is load-bearing: an anchored build
        # compiles a long-lived trust anchor into the native library and the
        # import-time check requires the signature key to equal it, so
        # re-signing edited sources with another key no longer verifies; an
        # unanchored build is signed with a per-build ephemeral key and is
        # self-referential.  The unanchored path is a deliberate accommodation
        # for FORKS and for the canonical repo BEFORE the key is installed --
        # but nothing stopped a real tag on the canonical repository from
        # taking it silently: verify-anchor is skipped when the variable is
        # empty, build-wheels accepts the skip, and the wheel smoke test did
        # not check anchoring either (audit H3).
        #
        # This step runs on EVERY tag push (not only when it will fail) so it
        # can never silently skip the way the check it replaces effectively
        # did; it prints the status every time and fails closed only on the
        # canonical repository with no anchor configured.  GitHub repository
        # names are case-insensitive, so both sides are lower-cased first.
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        env:
          ANCHOR_PUBKEY: ${{ vars.AMA_INTEGRITY_TRUST_ANCHOR_PUBKEY_HEX }}
          CANONICAL_REPO: Steel-SecAdv-LLC/AMA-Cryptography
        run: |
          set -euo pipefail
          if [ -n "${ANCHOR_PUBKEY}" ]; then
            echo "Integrity anchoring: ANCHORED — trust-anchor variable is set for ${GITHUB_REPOSITORY}."
            exit 0
          fi
          repo_lc=$(printf '%s' "${GITHUB_REPOSITORY}" | tr '[:upper:]' '[:lower:]')
          canon_lc=$(printf '%s' "${CANONICAL_REPO}" | tr '[:upper:]' '[:lower:]')
          if [ "${repo_lc}" = "${canon_lc}" ]; then
            echo "::error::Refusing to release ${GITHUB_REF##*/} from ${GITHUB_REPOSITORY} with no integrity trust anchor."
            echo "         AMA_INTEGRITY_TRUST_ANCHOR_PUBKEY_HEX is empty, so every wheel would ship a per-build"
            echo "         ephemeral signing key and self-referential integrity — a re-sign of edited sources would"
            echo "         verify, which is exactly what the anchor exists to prevent. Set the variable (Settings ->"
            echo "         Secrets and variables -> Actions -> Variables) together with the AMA_INTEGRITY_SIGNING_SEED_HEX"
            echo "         secret before tagging, or cut the release from a fork if an unanchored build is intended."
            exit 1
          fi
          echo "Integrity anchoring: UNANCHORED on non-canonical repository ${GITHUB_REPOSITORY} — the supported fork / pre-anchor path."

      - name: Run version consistency check
        # Cross-validate that every other version anchor (CMakeLists.txt,
        # include/ama_cryptography.h, docs/conf.py, Dockerfiles, etc.)
        # agrees with pyproject.toml.  This is the same check ci.yml
        # already runs; gating the release on it again is the
        # belt-and-suspenders pattern INVARIANT-11 expects.
        run: python tools/check_version_consistency.py

      - name: Refuse a tag whose own docs still call the release unreleased (M12)
        # The gates above check the tag SHAPE, the tag↔pyproject version and
        # cross-file version consistency — but none checks whether the shipped
        # prose still says the release has not happened.  A tag pushed while
        # CHANGELOG.md carries "## [x.y.z] - Unreleased", or the README says
        # "v5.0.0 is not tagged yet", ships a document that contradicts the tag
        # (audit M12).  Tag-state markers become false the instant this tag
        # exists (the push triggered this run), so they are enforced always.
        # The PyPI availability rows are decoupled: publish-pypi is opt-in on
        # vars.AMA_PUBLISH_TO_PYPI, so on a release that does NOT publish to PyPI
        # those rows are correct and must not be flipped — they are enforced with
        # --require-published only when this run does publish.
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
          PUBLISH_TO_PYPI: ${{ vars.AMA_PUBLISH_TO_PYPI }}
        run: |
          set -euo pipefail
          if [ "${PUBLISH_TO_PYPI}" = "true" ]; then
            python tools/check_release_state.py --version "${RELEASE_VERSION}" --require-published
          else
            python tools/check_release_state.py --version "${RELEASE_VERSION}"
          fi

      - name: Regenerate + check SBOM
        # Audit Issue 2: SBOM is driven from pyproject.toml.  We
        # regenerate fresh and require the committed copy match,
        # exactly the same way security.yml::sbom does.
        run: python tools/generate_sbom.py --check

  # Confirm the installed signing seed derives the configured trust anchor
  # BEFORE anything expensive runs.  Without this gate a mismatched pair is
  # first noticed by `_build_sign` inside cibuildwheel, i.e. after the whole
  # matrix has compiled the native library on every platform — a ~40 minute
  # round trip to learn that a repository variable is wrong.  Here it costs one
  # ubuntu build.
  #
  # Skipped when no anchor is configured, which is the supported unanchored
  # path (forks, and repositories before the key is installed); `build-wheels`
  # below treats "skipped" as permission to proceed.  The check itself lives in
  # integrity-anchor-check.yml so the dispatchable job and this gate cannot
  # drift apart.
  verify-anchor:
    name: Verify the integrity trust anchor pair
    needs: [preflight]
    if: vars.AMA_INTEGRITY_TRUST_ANCHOR_PUBKEY_HEX != ''
    uses: ./.github/workflows/integrity-anchor-check.yml
    # Forward ONLY the seed the anchor check reads, not the whole secret set
    # (`secrets: inherit`) — this is the one job handling long-lived signing-key
    # material, so it gets least privilege.  The trust-anchor value it also
    # reads is a repo variable (vars.*), inherited automatically.
    secrets:
      AMA_INTEGRITY_SIGNING_SEED_HEX: ${{ secrets.AMA_INTEGRITY_SIGNING_SEED_HEX }}

  build-wheels:
    name: cibuildwheel ${{ matrix.os }}
    needs: [preflight, verify-anchor]
    # `verify-anchor` is conditional, and a skipped dependency would otherwise
    # skip this job too.  Accept success or skipped, refuse failure.  Bracket
    # indexing for the hyphenated job id, for the reason documented on
    # `github-release` below.
    if: >-
      !cancelled()
      && needs.preflight.result == 'success'
      && needs['verify-anchor'].result != 'failure'
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        # RUNNER LABELS ARE RELEASE-CRITICAL.  This matrix carried `macos-13`
        # long after GitHub retired that image; the job never got a runner,
        # sat queued until `timeout-minutes`, and failed build-wheels — and
        # with it every downstream release stage.  Nothing caught it because
        # release.yml only runs on a tag push.
        #
        # `macos-15-intel` (x86_64) and `macos-15` (arm64) are the current
        # generally-available macOS labels; `macos-14` is marked deprecated
        # upstream and was the next latent outage in this same matrix.  Both
        # architectures need their own runner because CIBW_ARCHS_MACOS=auto64
        # builds for the host architecture only.
        # tools/check_workflow_commands.py fails the build on retired labels.
        os: [ubuntu-latest, ubuntu-24.04-arm, macos-15-intel, macos-15, windows-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.11"

      - name: Build wheels via cibuildwheel
        # Real v4.1.1 commit, verified with
        #   git ls-remote https://github.com/pypa/cibuildwheel.git refs/tags/v4.1.1
        # (This block previously said v3.2.0 and quoted the v3.2.0^{} lookup,
        # left behind when the pin was bumped; the SHA below is the v4.1.1 tag
        # — 7c619ef… is v3.2.0^{} — and the trailing marker was already
        # correct, which is why check_action_pins.py never flagged it.)
        #
        # Background, kept because it explains a real outage: an earlier pin
        # (e9c4a96e…) did not exist in pypa/cibuildwheel at all — it matched
        # neither the v3.2.0 tag object nor its commit — so every wheel job
        # aborted with "Unable to resolve action … unable to find version".
        # That is why no release before then produced artifacts.
        uses: pypa/cibuildwheel@4726cd35bb13f7bde50cf2761f2499ac7b3aa32c  # v4.1.1
        env:
          # PYTHON VERSION MATRIX:
          # Build for the same Python range the test workflows already
          # cover (3.10–3.14).  That equality is the point: a wheel for an
          # interpreter no CI lane exercises ships untested, so cp314 was
          # added to ci.yml and ci-build-test.yml in the same commit as
          # this line. Keep the three in step.
          #
          # The equality is per (runner architecture x interpreter), not
          # merely per Python range — this matrix ships five OS/arch rows,
          # and each one must have a pytest lane somewhere: linux x86_64 and
          # windows run all five interpreters in ci.yml AND ci-build-test;
          # linux aarch64 runs all five in ci.yml's ubuntu-24.04-arm
          # includes; macOS arm64 (macos-latest) and macOS x86_64
          # (macos-15-intel) each run all five in ci-build-test.  Before
          # the aarch64 includes were completed and the macos-15-intel
          # column existed, 8 of these 25 wheels — every macOS x86_64
          # wheel and aarch64 cp310/cp312/cp314 — shipped with only
          # cibuildwheel's import smoke test.  Extend the test matrices in
          # the same commit as any extension here.
          #
          # Skip PyPy and 32-bit musllinux variants; the native build under
          # setup.py CMakeBuild is x86-64 / arm64 only, and the ctypes shims
          # in ama_cryptography/ don't gain anything from PyPy's tracing JIT.
          CIBW_BUILD: "cp310-* cp311-* cp312-* cp313-* cp314-*"
          CIBW_SKIP: "pp* *_i686 *_ppc64le *_s390x *-musllinux_*"
          CIBW_ARCHS_LINUX: "auto64"
          CIBW_ARCHS_MACOS: "auto64"
          CIBW_ARCHS_WINDOWS: "AMD64"
          # WHEEL BUILD ENVIRONMENT:
          # AMA_BUILD_PIPELINE=1 enables setup.py's post-build integrity
          # signer.  The native-PQC and constant-time-AES guarantees are
          # NOT set here — they are enforced closer to the compiler, which
          # is why nothing needs to repeat them at this layer:
          #   * INVARIANT-7  — setup.py passes -DAMA_USE_NATIVE_PQC=ON
          #                    unconditionally, and CMakeLists.txt defaults
          #                    the option ON.
          #   * INVARIANT-20 — CMakeLists.txt defaults AMA_AES_CONSTTIME=ON
          #                    and *fails configuration* if it is turned off
          #                    without an explicit -DAMA_AES_TABLE_INSECURE=ON
          #                    acknowledgement, so a table-S-box wheel cannot
          #                    be produced by accident.
          # AMA_KYBER_BUILD_DIAGNOSTICS is INTENTIONALLY left unset so the
          # production wheels ship without the ~600 lines of printf-emitting
          # Kyber debug code (audit Issue 7).
          #
          # INTEGRITY TRUST ANCHOR (optional, opt-in per repository).
          # When the AMA_INTEGRITY_TRUST_ANCHOR_PUBKEY_HEX *variable* and the
          # AMA_INTEGRITY_SIGNING_SEED_HEX *secret* are both configured, the
          # wheel is built with the anchor compiled into the native library
          # (setup.py forwards it to CMake) and signed with the matching
          # long-lived key, and AMA_INTEGRITY_REQUIRE_TRUST_ANCHOR=1 makes the
          # signer refuse to emit an unanchored artefact.  That combination is
          # what makes a released wheel tamper-evident: the import-time check
          # then requires the signature's public key to equal a value baked
          # into the compiled .so, so re-signing edited .py files with an
          # attacker-chosen key no longer verifies.
          #
          # When they are NOT configured, both expand to empty and the build
          # behaves exactly as before — a per-build ephemeral signing key, an
          # unanchored (still signed) artefact.  Releases therefore keep
          # working on a fork or before the key is installed, rather than
          # failing on a missing secret.  Run the "Integrity anchor check"
          # workflow after installing the key to confirm the pair matches.
          CIBW_ENVIRONMENT: >-
            AMA_BUILD_PIPELINE=1
            CMAKE_BUILD_PARALLEL_LEVEL=4
          # The three integrity variables are supplied through the step
          # environment below and forwarded into the Linux build container by
          # name, NOT interpolated into CIBW_ENVIRONMENT.
          #
          # What this does and does not buy, stated accurately: it keeps the
          # seed out of the CIBW_ENVIRONMENT *string* — out of this file, and
          # out of anything that echoes that option verbatim.  It does NOT
          # keep it out of cibuildwheel's resolved build options; those merge
          # the passed-through variables back in, so the value is present
          # there either way, and GitHub's secret masking is what redacts it
          # in the log.  (An earlier revision of this comment claimed the
          # pass-by-name kept it out of the printed configuration entirely.
          # It does not — checked against the resolver in the pinned 4.1.1.)
          # The remaining benefit is real but narrow: one fewer place the
          # literal seed is written.
          #
          # On macOS and Windows cibuildwheel builds in the host environment,
          # so the step-level `env` below already reaches the build there.
          CIBW_ENVIRONMENT_PASS_LINUX: >-
            AMA_INTEGRITY_TRUST_ANCHOR_PUBKEY_HEX
            AMA_INTEGRITY_SIGNING_SEED_HEX
            AMA_INTEGRITY_REQUIRE_TRUST_ANCHOR
          AMA_INTEGRITY_TRUST_ANCHOR_PUBKEY_HEX: ${{ vars.AMA_INTEGRITY_TRUST_ANCHOR_PUBKEY_HEX }}
          AMA_INTEGRITY_SIGNING_SEED_HEX: ${{ secrets.AMA_INTEGRITY_SIGNING_SEED_HEX }}
          AMA_INTEGRITY_REQUIRE_TRUST_ANCHOR: ${{ vars.AMA_INTEGRITY_TRUST_ANCHOR_PUBKEY_HEX != '' && '1' || '' }}
          # PRE-BUILD COMMANDS — install cmake + the cython/numpy build
          # deps that setup.py CMakeBuild needs.  cibuildwheel runs in
          # an isolated build env that only sees pyproject.toml's
          # [build-system].requires, so any missing build-time dep
          # fails the wheel build closed.
          #
          # Each requirement specifier is single-quoted so the cibuildwheel
          # subshell doesn't interpret `>=` as output redirection
          # (Copilot review #322 — without the quotes, `pip install
          # cmake>=4.4.0` runs as `pip install cmake` plus `>` redirect
          # to a file named `=4.4.0`, silently floating to whatever
          # pip resolves and missing the INVARIANT-8 toolchain floor).
          # Floors match pyproject.toml [build-system].requires + setup.py's
          # preflight so all three build-dependency sources stay in lockstep.
          #
          # The Windows variant uses DOUBLE quotes.  cibuildwheel runs
          # before-build commands through `cmd.exe` on Windows, where a single
          # quote is an ordinary character rather than a quoting operator, so
          # the POSIX form reached pip verbatim and every Windows wheel job
          # died on `Invalid requirement: "'cmake"`.  Double quotes are honoured
          # by cmd.exe *and* still shield `>=` from being read as redirection,
          # which is the property the quoting exists for in the first place.
          CIBW_BEFORE_BUILD_LINUX: "pip install 'cmake>=4.4.0' 'cython>=3.2.8' 'numpy>=1.24.0'"
          CIBW_BEFORE_BUILD_MACOS: "pip install 'cmake>=4.4.0' 'cython>=3.2.8' 'numpy>=1.24.0'"
          CIBW_BEFORE_BUILD_WINDOWS: 'pip install "cmake>=4.4.0" "cython>=3.2.8" "numpy>=1.24.0"'
          # MANYLINUX BASELINE: manylinux_2_28 has GCC 12 baseline (the
          # INVARIANT-8 minimum), AVX-512 intrinsics, and is supported
          # by every modern pip.
          CIBW_MANYLINUX_X86_64_IMAGE: "manylinux_2_28"
          CIBW_MANYLINUX_AARCH64_IMAGE: "manylinux_2_28"
          # WINDOWS WHEEL REPAIR: disabled on purpose.
          #
          # cibuildwheel 4.0 made `delvewheel repair` the DEFAULT Windows
          # repair command (it did not run in 3.x — that is how v3.0.0–v3.4.0
          # shipped working Windows wheels).  Bumping the pin from v3.2.0 to
          # v4.1.1 in the pre-v3.5.0 dependency sweep silently turned it on,
          # and it took the v3.5.0 release down: the five wheel jobs all BUILT
          # green, then the Windows job died in the new repair step with
          #     FileNotFoundError: Unable to find library: ama_cryptography.dll
          # Because build-wheels failed, hash → provenance → sign →
          # publish-pypi → github-release were all skipped, so the pushed
          # v3.5.0 tag produced no GitHub Release at all.
          #
          # The DLL is not missing — it is already vendored INTO the wheel,
          # next to the Cython .pyd bindings, by setup.py's
          # CMakeBuild._copy_native_library_into_package (D-1).  delvewheel
          # raises anyway: at its default settings it resolves each .pyd's
          # `ama_cryptography.dll` import against PATH only (wheel_dirs=None in
          # delvewheel/_wheel_repair.py), not the .pyd's own package directory,
          # and the project's own DLL is on neither PATH.
          #
          # We must NOT "fix" this by letting delvewheel vendor + name-mangle
          # the DLL.  The native library is loaded at runtime by FIXED name —
          # `ama_cryptography.dll` / `libama_cryptography.dll` — through ctypes
          # in ama_cryptography/pqc_backends.py::_find_native_library.  A
          # delvewheel-mangled name (ama_cryptography-<hash>.dll) would patch
          # the .pyd import tables but leave that ctypes-by-name load unable to
          # find the library, silently dropping the entire native crypto
          # backend to the pure-Python fallback.
          #
          # No repair is needed on Windows: the vendored DLL sits beside the
          # .pyd files, so the loader resolves the .pyd import via
          # LOAD_WITH_ALTERED_SEARCH_PATH (the module's own directory is
          # searched first) and the ctypes path loads it by absolute path.
          # An empty command skips repair on Windows only; Linux (auditwheel)
          # repair is untouched and still runs.  macOS repair is disabled
          # separately below, for a related reason the 5.0.0 release dry run
          # surfaced.
          # tools/check_workflow_commands.py already tracks
          # CIBW_REPAIR_WHEEL_COMMAND_WINDOWS in WINDOWS_COMMAND_KEYS; an empty
          # value has no payload or quoting for it to flag.
          CIBW_REPAIR_WHEEL_COMMAND_WINDOWS: ""
          # MACOS WHEEL REPAIR: disabled on purpose, for the same reason as
          # Windows above and for a second one this release introduced.
          #
          # Found by the mandatory release dry run, which is the only thing
          # that runs this path: the macos-15 and macos-15-intel wheels BUILT
          # and SIGNED green and then failed their own smoke test with
          #     dilithium_binding.cpython-310-darwin.so: digest MISMATCH
          #     ed25519_binding... hkdf_binding... hmac_binding... sha3_binding...
          # every one of the five binding extensions.  `delocate-wheel` runs
          # AFTER the integrity signer, and it rewrites each extension's Mach-O
          # load commands — so the bytes the wheel ships are, by construction,
          # not the bytes the artefact signed.  Since 5.0.0 binds those digests
          # (v3 artefact), that is a hard import failure on every macOS wheel.
          # The check is working; the pipeline ordering was not.
          #
          # There is nothing for delocate to do here.  This project links no
          # external libraries — INVARIANT-1, zero external crypto
          # dependencies — so the only shared object involved is
          # `libama_cryptography.dylib`, which setup.py's
          # CMakeBuild._copy_native_library_into_package already vendors INTO
          # the package beside the .so bindings, resolved via `@loader_path`.
          # And as on Windows, letting the repair tool vendor + name-mangle it
          # would be actively harmful: `pqc_backends._find_native_library`
          # loads the library through ctypes by FIXED name, so a mangled
          # `libama_cryptography-<hash>.dylib` would silently drop the native
          # backend even though the bindings themselves still resolved.
          #
          # The wheel smoke test is the proof that skipping repair is safe: it
          # imports the built wheel in a throwaway venv, asserts the native
          # backend loaded and that nothing degraded to a fallback, and — added
          # with this change — asserts explicitly that every shipped binding
          # extension is covered by the signature and matches it, so a future
          # post-signing rewrite is reported as what it is rather than as an
          # opaque import failure.
          CIBW_REPAIR_WHEEL_COMMAND_MACOS: ""
          # LINUX WHEEL REPAIR: default repair preserved, then RE-SIGN.
          #
          # The integrity artefact is minted by setup.py's post-build signer,
          # BEFORE this repair step — so a repair tool that rewrites the
          # binding ELFs after signing invalidates the signature on every
          # wheel it touches.  That is not hypothetical: it is exactly what
          # delocate did on macOS in the 5.0.0 release dry run (see the MACOS
          # block above), and today's auditwheel leaving the bindings alone
          # (verified on the v4.0.0 assets: nothing grafted, the library
          # resolves in-package via $ORIGIN) is a property of the current
          # auditwheel, not a guarantee.  Instead of betting the release on
          # that staying true, keep the DEFAULT Linux repair (the first half
          # below is cibuildwheel's own default command — manylinux retagging
          # is worth having) and then re-sign each repaired wheel over its
          # POST-repair bytes: tools/resign_wheel.py unpacks the wheel, runs
          # the signer against the unpacked tree (with a guard that the bound
          # native library came from INSIDE the wheel), regenerates RECORD and
          # repacks in place.  The binding digests then describe what ships by
          # construction, so the failure mode delocate exhibited cannot
          # invalidate a shipped Linux signature no matter what a future
          # auditwheel rewrites.  CIBW_TEST_COMMAND's smoke test
          # (check_binding_extensions_are_bound) still verifies every
          # re-signed wheel end to end.  macOS/Windows repair stay disabled
          # above — nothing here re-enables them.
          CIBW_REPAIR_WHEEL_COMMAND_LINUX: "auditwheel repair -w {dest_dir} {wheel} && python {project}/tools/resign_wheel.py {dest_dir}"
          # TEST COMMAND: smoke-test the built wheel inside the throwaway
          # virtualenv cibuildwheel creates for it.  The full pytest suite
          # runs in ci-build-test.yml; this gate answers the narrower
          # question only a packaged build can answer — did the native
          # extension load, did anything degrade to a fallback backend,
          # does every shipped primitive round-trip and reject tampering.
          #
          # This is a FILE, not an inline `python -c`.  It used to be a
          # folded YAML scalar (`>-`), which joins the block's lines with a
          # space — so the payload reaching the interpreter began with a
          # leading space and every wheel on every platform died with
          # `IndentationError: unexpected indent` after building perfectly.
          # A script has no folding or shell-quoting layer between what is
          # written and what runs, and it can be executed locally.
          # tools/check_workflow_commands.py compiles any remaining inline
          # `python -c` payload so this cannot regress silently.
          CIBW_TEST_COMMAND: "python {project}/tools/wheel_smoke_test.py"

      - name: Upload wheel artefacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: wheels-${{ matrix.os }}
          path: ./wheelhouse/*.whl
          retention-days: 30

  build-sdist:
    name: Build sdist
    needs: [preflight]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.11"

      - name: Install build
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build sdist
        run: python -m build --sdist

      - name: Install the sdist from source and smoke-test it
        # audit B3/H6.  The sdist is the artefact this release signs, attests
        # and publishes to PyPI, and the channel every user on --no-binary or a
        # platform without a matching wheel takes.  It shipped uninstallable
        # before MANIFEST.in existed (no CMakeLists.txt, no src/c, no .pyx), and
        # nothing in the pipeline ever tried to install it.  Build the wheel
        # FROM the tarball in a throwaway venv and run the release smoke test
        # against the installed distribution, so a missing build input is a red
        # build here rather than a signed, attested, unbuildable release.
        run: |
          set -euxo pipefail
          python -m venv /tmp/sdist-venv
          /tmp/sdist-venv/bin/python -m pip install --upgrade pip
          /tmp/sdist-venv/bin/pip install dist/*.tar.gz
          # Run from OUTSIDE the checkout so wheel_smoke_test.py's source-tree
          # guard resolves `import ama_cryptography` to the installed wheel.
          ( cd /tmp && /tmp/sdist-venv/bin/python "$GITHUB_WORKSPACE/tools/wheel_smoke_test.py" )

      - name: Upload sdist artefact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sdist
          path: dist/*.tar.gz
          retention-days: 30

  # =========================================================================
  # Aggregate artefact digests — single SLSA provenance attestation over
  # the union of wheels + sdist.  The slsa-github-generator workflow
  # accepts a base64-encoded JSON document of {"name", "digest"} pairs;
  # we compute that here so the provenance job is matrix-free and the
  # attestation file is one .intoto.jsonl rather than one per OS.
  # =========================================================================
  hash-artefacts:
    name: Hash artefacts for SLSA subject set
    needs: [build-wheels, build-sdist]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      subjects: ${{ steps.hash.outputs.subjects }}
    steps:
      - name: Download wheel artefacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          path: dist
          pattern: wheels-*
          merge-multiple: true

      - name: Download sdist artefact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: sdist
          path: dist

      - name: Compute base64 subjects
        id: hash
        # `shell: bash` is REQUIRED, not cosmetic.  The default shell for a
        # `run:` block is `bash -e {0}` — which has `-e` but NOT `pipefail`.
        # In `sha256sum ... | sort > subjects.txt` the pipeline's status is
        # `sort`'s, and `sort` exits 0 on empty input.  So if either
        # download-artifact step yielded nothing, sha256sum failed, `-e` did
        # not fire, subjects.txt was empty, and the base64 of an empty file
        # was handed to slsa-github-generator as the subject set: green
        # provenance attesting to NOTHING, on a release tag.  `shell: bash`
        # selects `bash --noprofile --norc -eo pipefail {0}`, so the failing
        # sha256sum now fails the step.  The explicit emptiness assertion
        # below is the second line of defence, because a subject set that is
        # empty for ANY reason must never reach the generator.
        shell: bash
        run: |
          # The SLSA generator expects a single base64-encoded string
          # containing one "DIGEST  FILENAME" line per artefact, in
          # the same shape sha256sum prints natively.  We sort to make
          # the output deterministic across re-runs.
          cd dist
          sha256sum *.whl *.tar.gz | LC_ALL=C sort > ../subjects.txt
          cd ..
          cat subjects.txt
          if [ ! -s subjects.txt ]; then
            echo "::error::subjects.txt is empty — no wheel or sdist artefact was" \
                 "downloaded, so the SLSA provenance would attest to nothing." >&2
            exit 1
          fi
          subjects_b64=$(base64 --wrap=0 subjects.txt)
          if [ -z "$subjects_b64" ]; then
            echo "::error::base64 subject encoding is empty; refusing to emit an" \
                 "empty SLSA subject set." >&2
            exit 1
          fi
          echo "subjects=$subjects_b64" >> "$GITHUB_OUTPUT"

  provenance:
    name: SLSA v1 provenance attestation
    needs: [hash-artefacts]
    permissions:
      id-token: write       # OIDC for the keyless attestation
      # The slsa-github-generator reusable workflow's internal upload-assets
      # job statically declares `contents: write`.  A called workflow cannot be
      # granted more than the caller allows, so `contents: read` here fails the
      # whole run at startup ("nested job 'upload-assets' is requesting
      # 'contents: write', but is only allowed 'contents: read'") — even though
      # upload-assets is false.  The generator's documented caller permissions
      # require write; grant it here (the top-level default stays read).
      contents: write
      actions: read
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.1.0
    with:
      base64-subjects: ${{ needs.hash-artefacts.outputs.subjects }}
      provenance-name: "ama-cryptography.intoto.jsonl"
      upload-assets: false

  # =========================================================================
  # Sigstore signing (sigstore-python) — keyless, OIDC-bound.  No long-
  # lived signing key sits on the runner.  The signature .sig + the
  # public certificate .crt ship alongside the wheel; verification uses
  # `sigstore verify identity`.
  # =========================================================================
  sign:
    name: Sigstore sign (wheels + sdist)
    needs: [build-wheels, build-sdist]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download wheel artefacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          path: dist
          pattern: wheels-*
          merge-multiple: true

      - name: Download sdist artefact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: sdist
          path: dist

      - name: Sign distribution with sigstore-python
        uses: sigstore/gh-action-sigstore-python@5b79a39c381910c090341a2c9b0bf022c8b387e1  # v3.4.0
        with:
          inputs: ./dist/*.whl ./dist/*.tar.gz
          upload-signing-artifacts: true
          release-signing-artifacts: false

      - name: Upload signed bundle
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: signed-distribution
          path: |
            dist/*.whl
            dist/*.tar.gz
            dist/*.sigstore
            dist/*.sigstore.json
          retention-days: 90

  # =========================================================================
  # PyPI Trusted Publishing — no long-lived token.  Requires the PyPI
  # project to have a "trusted publisher" configured for this repo /
  # workflow / environment trio.  The `environment: pypi` block is the HOOK
  # for a maintainer-approval barrier between a tag push and a published
  # release — but, exactly as the `release` environment note on github-release
  # below records, referencing an environment creates it WITHOUT protection
  # rules: the required-reviewer protection must be configured once on the
  # repository's Settings -> Environments page for the approval to be
  # enforced.  Until it is, this block documents the intended gate rather than
  # enforcing it (audit M10).
  # =========================================================================
  publish-pypi:
    name: Publish to PyPI (Trusted Publishing)
    needs: [preflight, sign, provenance]
    # Publish ONLY on tag pushes.  A manually-dispatched run from an
    # arbitrary branch ref bypasses the preflight tag-vs-pyproject
    # version check (which only fires under `github.event_name ==
    # 'push'`), so allowing workflow_dispatch + dry_run=false to
    # publish would let any branch land an unverified release on
    # PyPI.  Copilot review #322: restrict to `refs/tags/v*`.  The
    # workflow_dispatch dry-run path still exercises the entire
    # build/sign/provenance flow — it just stops before the upload.
    #
    # PyPI publishing is OPT-IN, gated on the repository variable
    # AMA_PUBLISH_TO_PYPI. Distributing through PyPI is a deliberate
    # operational choice, not a precondition for cutting a release: this
    # project also ships via git tags, GitHub Release assets and a private
    # index (see README "Distribution Channels"). Before this gate, a tag
    # pushed without a configured trusted publisher failed here AND took
    # github-release down with it, so the project could not produce a
    # GitHub Release at all without first committing to PyPI.
    #
    # Set the variable to 'true' (Settings -> Secrets and variables ->
    # Actions -> Variables) once the PyPI trusted publisher and the `pypi`
    # environment both exist. Leaving it unset skips publishing; the skip is
    # stated in the release notes rather than passing silently.
    if: >-
      github.event_name == 'push'
      && startsWith(github.ref, 'refs/tags/v')
      && vars.AMA_PUBLISH_TO_PYPI == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: pypi
      url: https://pypi.org/project/ama-cryptography/${{ needs.preflight.outputs.version }}/
    permissions:
      id-token: write       # required for trusted publishing OIDC
    steps:
      - name: Download signed distribution
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: signed-distribution
          path: dist

      - name: Filter to wheels + sdist (drop sigstore files)
        # pypa/gh-action-pypi-publish only wants the actual distributions,
        # not the .sigstore signature blobs that ride alongside them.
        # We move the signatures out of dist/ before the upload step.
        run: |
          mkdir -p signatures
          mv dist/*.sigstore* signatures/ 2>/dev/null || true
          ls -la dist/
          ls -la signatures/

      - name: Publish to PyPI (Trusted Publishing)
        uses: pypa/gh-action-pypi-publish@ba38be9e461d3875417946c167d0b5f3d385a247  # v1.14.1
        with:
          packages-dir: dist/
          # No `password` — Trusted Publishing handles OIDC exchange.
          # Skip-existing avoids breaking on re-runs of a partial
          # publish, but if a wheel has actually changed bytes since
          # the prior partial upload, that's a worse problem we want
          # to surface, not skip.  Off by default; flip to true only
          # after the first failed-mid-upload incident.
          skip-existing: false
          verbose: true

  github-release:
    name: Create GitHub Release
    # MUST depend on publish-pypi (Copilot review #322).  Without it,
    # a tag push that succeeds at sign+provenance but fails at PyPI
    # upload (rejected by the trusted-publisher rule, environment
    # approval timeout, transient PyPI 5xx) would still publish the
    # GitHub Release with body text "Published to PyPI" — false
    # advertising and a credibility hit.  Sequencing via `needs` makes
    # the release announcement strictly downstream of the PyPI upload.
    needs: [preflight, sign, provenance, publish-pypi]
    #
    # The original guard was a bare `needs: [... publish-pypi]`, which is
    # correct about the hazard (never announce a PyPI upload that failed) but
    # conflates two different outcomes. `needs` treats SKIPPED the same as
    # FAILED, so with PyPI publishing opt-in (see publish-pypi above) a
    # deliberately unconfigured PyPI would silently suppress the GitHub
    # Release too — no assets, no signatures, no provenance published, for a
    # tag that built everything successfully.
    #
    # This condition keeps the safety property and drops the conflation:
    #   * publish-pypi FAILED   -> no release  (the original hazard)
    #   * publish-pypi SKIPPED  -> release proceeds, notes say PyPI was skipped
    #   * sign / provenance not successful -> no release, unconditionally
    #
    # Bracket indexing, not dot notation: `needs.publish-pypi.result` asks an
    # expression grammar in which `-` is also subtraction to read a hyphenated
    # job id as a single identifier.  No other workflow here dereferences a
    # hyphenated job name, so there is no in-repo evidence the dot form is
    # safe, and this file only ever executes on a tag push — the one place a
    # wrong guess is discovered at the worst moment.  `needs['publish-pypi']`
    # is unambiguous under any reading.
    if: >-
      !cancelled()
      && github.event_name == 'push'
      && startsWith(github.ref, 'refs/tags/v')
      && needs.sign.result == 'success'
      && needs.provenance.result == 'success'
      && needs['publish-pypi'].result != 'failure'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # The release narrative has always said publication happens "under the
    # protected `release` environment", and until this block existed the
    # workflow disagreed: `publish-pypi` carried `environment: pypi` while
    # the job that actually creates the immutable release — the one holding
    # `contents: write` — was gated by nothing but its `if:`.  Referencing
    # the environment here creates it on first run WITHOUT protection
    # rules; the required-reviewer / branch-and-tag protections must be
    # configured once on the repository's Settings -> Environments page for
    # the gate to hold anything.  Until they are, this line documents the
    # intended gate rather than enforcing it, and the release runbook above
    # treats configuring it as part of the tag-time procedure.
    environment:
      name: release
      url: https://github.com/${{ github.repository }}/releases/tag/v${{ needs.preflight.outputs.version }}
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Download all artefacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          path: artefacts

      - name: Resolve the PyPI line for the release notes
        id: pypi_line
        # Computed in a step rather than inline in the release body.  The
        # inline form needed a multi-line ${{ }} expression inside a `|` block
        # scalar — syntax that nothing in CI would ever exercise, because this
        # workflow only runs on a tag push.  That is the same class of defect
        # (invisible until release day) that INVARIANT-25 exists to catch, so
        # it does not belong in the fix for it.  A shell conditional over a
        # single simple expression is testable by reading it.
        env:
          PYPI_RESULT: ${{ needs['publish-pypi'].result }}
        run: |
          if [ "$PYPI_RESULT" = "success" ]; then
            line="published via Trusted Publishing (no long-lived token)."
          else
            line="not published — PyPI distribution is not enabled for this repository. Install from this release's assets, a git tag, or the private index (see README, Distribution Channels)."
          fi
          echo "line=$line" >> "$GITHUB_OUTPUT"
          echo "PyPI publish result: ${PYPI_RESULT:-<none>}"

      - name: Resolve the integrity anchoring line for the release notes
        id: anchor_line
        # Audit H3: the release notes must STATE whether the wheels are anchored,
        # the same way the PyPI line above states whether they were published.
        # The signal is the trust-anchor repository VARIABLE — the identical
        # value that decides whether build-wheels compiled an anchor into the
        # native library — so the notes describe what the assets actually are.
        # Computed in a step, not inline, for the same reason as the PyPI line.
        env:
          ANCHOR_PUBKEY: ${{ vars.AMA_INTEGRITY_TRUST_ANCHOR_PUBKEY_HEX }}
        run: |
          if [ -n "$ANCHOR_PUBKEY" ]; then
            line="anchored — the import-time integrity check requires the signature key to equal a trust anchor compiled into the native library, so re-signing edited sources with another key does not verify."
          else
            line="unanchored — signed with a per-build ephemeral key and self-referential (tamper-evident against corruption, not against a re-sign). The supported fork / pre-anchor path; see SECURITY.md, Module Integrity Verification."
          fi
          echo "line=$line" >> "$GITHUB_OUTPUT"
          echo "integrity anchoring for notes: ${ANCHOR_PUBKEY:+anchored}${ANCHOR_PUBKEY:+ }${ANCHOR_PUBKEY:-unanchored}"

      - name: Aggregate release assets
        run: |
          mkdir -p release-assets
          # Wheels & sdist
          find artefacts -name '*.whl' -exec cp {} release-assets/ \;
          find artefacts -name '*.tar.gz' -exec cp {} release-assets/ \;
          # Sigstore signatures
          find artefacts -name '*.sigstore*' -exec cp {} release-assets/ \;
          # SLSA provenance attestations
          find artefacts -name '*.intoto.jsonl' -exec cp {} release-assets/ \;
          # SBOM (committed)
          cp docs/compliance/sbom-c-library.json release-assets/
          ls -la release-assets/

          # audit H14: `find` exits 0 when it matches nothing, so an empty
          # artefacts/ tree (a broken download, a renamed upstream artefact, a
          # sign job whose output pattern drifted) would copy only the committed
          # SBOM and publish a release whose notes promise "signed wheels" and
          # "SLSA provenance" over an asset list of one JSON file — and the
          # release is IMMUTABLE, so it cannot be repaired. Assert every asset
          # CLASS is present, in the expected quantity, before the freeze.
          #
          # EXPECTED_WHEELS is the build-wheels matrix size: 5 CPython
          # (cp310-cp314) x 5 platform-arches (ubuntu-latest, ubuntu-24.04-arm,
          # macos-15-intel, macos-15, windows-latest). Update it here, under
          # review, whenever that matrix changes.
          EXPECTED_WHEELS=25
          n_whl=$(find release-assets -name '*.whl' | wc -l)
          n_sdist=$(find release-assets -name '*.tar.gz' | wc -l)
          n_sig=$(find release-assets -name '*.sigstore*' | wc -l)
          n_prov=$(find release-assets -name '*.intoto.jsonl' | wc -l)
          n_sbom=$(find release-assets -name 'sbom-c-library.json' | wc -l)
          echo "asset inventory: wheels=$n_whl sdist=$n_sdist signatures=$n_sig provenance=$n_prov sbom=$n_sbom"
          fail=0
          [ "$n_whl" -eq "$EXPECTED_WHEELS" ] || { echo "::error::expected $EXPECTED_WHEELS wheels, found $n_whl"; fail=1; }
          [ "$n_sdist" -eq 1 ] || { echo "::error::expected 1 sdist, found $n_sdist"; fail=1; }
          # sigstore signs every wheel and the sdist, so there is at least one
          # signature per distributable; fewer means a whole class went missing.
          [ "$n_sig" -ge "$((EXPECTED_WHEELS + 1))" ] || { echo "::error::expected >= $((EXPECTED_WHEELS + 1)) sigstore signatures, found $n_sig"; fail=1; }
          [ "$n_prov" -ge 1 ] || { echo "::error::expected >= 1 SLSA provenance attestation, found $n_prov"; fail=1; }
          [ "$n_sbom" -eq 1 ] || { echo "::error::expected the committed SBOM, found $n_sbom"; fail=1; }
          if [ "$fail" -ne 0 ]; then
            echo "::error::release asset inventory incomplete — refusing to publish an immutable release with a missing asset class"
            exit 1
          fi

      # IMMUTABLE RELEASES: assets freeze at publish, so every asset must be
      # attached while the release is still a draft.  At the pinned v3.0.1 this
      # holds without further configuration, and the ordering is worth stating
      # because it is the whole reason this step is safe:
      #
      #   * createRelease()  — `draft = prerelease === true ? input_draft : true`,
      #                        so a NON-prerelease is always created as a draft.
      #   * updateRelease()  — passes `draft: existingRelease.draft`, i.e. it
      #                        preserves the draft state rather than publishing
      #                        early, so reusing a hand-written draft is safe.
      #   * main.ts          — release() -> upload every file -> finalizeRelease(),
      #                        and only finalizeRelease() sets `draft: false`.
      #
      # Nothing else may attach assets to the release: the provenance job sets
      # `upload-assets: false` and the sign job sets
      # `release-signing-artifacts: false`, so this step is the single writer
      # and nothing can race the freeze.
      #
      # DO NOT HAND-CREATE A DRAFT RELEASE FOR THE TAG BEFOREHAND.  The action
      # locates an existing release with findTagFromReleases() -> getReleaseByTag(),
      # i.e. `GET /releases/tags/{tag}`, which GitHub documents as "Get a
      # PUBLISHED release with the specified tag".  A draft has no git tag, so
      # that call 404s, the action falls through to createRelease(), and the
      # repository ends up with two releases for one tag: the orphaned draft and
      # the one this workflow just made.  This step must own release creation.
      #
      # Long-form release notes are applied by EDITING THE PUBLISHED RELEASE
      # afterwards.  Immutable releases freeze the tag and the assets only — the
      # title and notes stay editable — so the write-up can land after publish
      # without weakening anything this pipeline guarantees.
      #
      # The two inputs below then protect that hand-edited text if this workflow
      # is ever re-run for the same tag (a re-run finds the now-published
      # release and takes the update path).  Without them a re-run silently
      # resets the title to the bare tag and overwrites the notes.
      - name: Create GitHub Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228  # v3.0.2
        with:
          tag_name: ${{ github.ref_name }}
          # `name:` is deliberately NOT set.  On the update path the action
          # resolves `input_name || existingRelease.name || tag`, so passing
          # `${{ github.ref_name }}` would reset a hand-edited release title
          # back to the bare tag ("v3.4.0") on any re-run.  Omitting it keeps
          # the edited title.  On the create path the action already falls back
          # to `input_name || tag` — the identical bare-tag title this
          # previously set explicitly — so a first run is unchanged.
          body: |
            AMA Cryptography ${{ needs.preflight.outputs.version }}

            * Wheels signed via sigstore (keyless, OIDC-bound).
            * SLSA v1 provenance attestations attached.
            * C-library SBOM (CycloneDX) attached; the Python SBOM is published per-commit by security.yml as the `sbom` workflow artifact.
            * PyPI: ${{ steps.pypi_line.outputs.line }}
            * Integrity: ${{ steps.anchor_line.outputs.line }}

            Verify with:

                $ pip install sigstore slsa-verifier
                $ sigstore verify identity --bundle DOWNLOAD.sigstore.json \
                    --cert-identity https://github.com/${{ github.repository }}/.github/workflows/release.yml@${{ github.ref }} \
                    --cert-oidc-issuer https://token.actions.githubusercontent.com \
                    DOWNLOAD.whl
                $ slsa-verifier verify-artifact DOWNLOAD.whl \
                    --provenance-path DOWNLOAD.intoto.jsonl \
                    --source-uri github.com/${{ github.repository }} \
                    --source-tag ${{ github.ref_name }}

            See the full release notes in CHANGELOG.md.
          files: release-assets/*
          # Protects hand-edited release notes across a re-run.  On the update
          # path the body otherwise resolves as
          # `workflowBody || existingReleaseBody` — the workflow body wins and
          # the edited notes are destroyed.  With this set, the existing body is
          # kept and the verification block above is appended to it
          # (`existingReleaseBody + "\n" + workflowBody`).  createRelease() does
          # not consult this input at all, so a first run is unchanged and still
          # gets the workflow body verbatim.
          append_body: true
          # Draft the PRERELEASE case only.  A non-prerelease resolves to false
          # and is published by finalizeRelease() once the assets are attached
          # (the v3.4.0 path).  A prerelease must stay a draft instead: the
          # action only auto-drafts non-prereleases, so a published pre-release
          # would be frozen by immutable releases before its assets uploaded and
          # the job would die on "Cannot upload assets to an immutable release".
          # This mirrors the guidance in the action's own `draft` input docs.
          # Consequence: a prerelease is left as a draft for a maintainer to
          # publish by hand, and it fires `release.published` rather than
          # `release.prereleased`.
          #
          # The flag comes from preflight, which derives it from the PEP 440
          # pyproject version (rcN/aN/bN/.devN) — the same string the tag must
          # equal.  It deliberately does NOT sniff github.ref_name for SemVer
          # `-rc`/`-alpha`/`-beta`: a PEP 440 tag (v5.0.0rc1) has no hyphen, so
          # that older test never fired and every rc would have shipped as a
          # full, non-draft release.
          draft: ${{ needs.preflight.outputs.is_prerelease == 'true' }}
          prerelease: ${{ needs.preflight.outputs.is_prerelease == 'true' }}
