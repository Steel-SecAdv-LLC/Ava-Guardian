# Copyright (C) 2025-2026 Steel Security Advisors LLC
# SPDX-License-Identifier: Apache-2.0
name: CI - Static Analysis (C Code)

on:
  push:
    branches: [ main, develop, 'feature/**', 'fix/**' ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Nightly extended sanitizer matrix at 04:00 UTC (audit Issue 9
    # close-out, promoted from weekly to nightly).  MSan and TSan have
    # higher false-positive rates than ASan+UBSan and a per-PR latency
    # cost we're not willing to pay, so we run them out of the
    # merge-critical path.  Nightly cadence shortens the regression
    # window from up-to-7-days to up-to-24-hours — the operational
    # cost is one extra runner-hour per night for the four scheduled
    # jobs (MSan, TSan, Valgrind, reproducible-build), which is in
    # the noise compared with the bounded-bisect benefit when a real
    # sanitiser regression does land.
    - cron: '0 4 * * *'
  # workflow_dispatch enables on-demand runs of the extended matrix —
  # several jobs below predicate on `github.event_name ==
  # 'workflow_dispatch'`, so the trigger MUST be declared here or the
  # advertised manual escape hatch is impossible to fire
  # (Copilot review #322).
  workflow_dispatch:

# security-events: write is granted on the codeql job alone, not here — a
# workflow-level grant hands SARIF-upload rights to every job in the file
# when exactly one needs it (least privilege, same rule as the other
# workflows' contents: read default).
permissions:
  contents: read

# Collapse overlapping runs on the same ref; main and scheduled runs
# (the weekly extended sanitizer matrix) are preserved.
concurrency:
  group: static-analysis-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' && github.event_name != 'schedule' }}

jobs:
  cppcheck:
    name: Cppcheck Static Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install cppcheck
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list /etc/apt/sources.list.d/azure-cli.list || true
          .github/scripts/apt-install.sh cppcheck

      - name: Run cppcheck on C sources
        # `shell: bash` is load-bearing: it adds `-o pipefail`, which the
        # default `bash -e {0}` does NOT set.  Without it the pipeline's status
        # is `tee`'s (always 0), so `--error-exitcode=1` was inert and this gate
        # could not fail on a finding — the same defect the clang-tidy job below
        # documents and guards against.
        shell: bash
        run: |
          # Scope and suppressions, all four established by running the gate for
          # the first time (with pipefail it reported 121 findings, none of them
          # a defect — see below).  A gate that is permanently red is as useless
          # as one that cannot fail, so the configuration below is the set that
          # makes a NEW finding meaningful:
          #
          # `style` is dropped.  Its 78 findings here are opinions —
          # variableScope ("this local could be declared inside the loop"),
          # unusedStructMember, constParameterPointer — not defects, and this
          # repository already runs clang-tidy (fail-closed), the clang static
          # analyzer, CodeQL, and -Wall -Wextra -Wpedantic as errors, which is
          # where substantive C analysis actually happens.  Keeping `style` at
          # --error-exitcode=1 is what made this gate un-turn-on-able.
          #
          # shiftTooManyBitsSigned: the `x >> 31` / `x >> 63` sign-broadcast
          # mask, which is how every branchless comparison in this codebase is
          # written (ama_dilithium.c, ama_secp256k1.c, ama_cpuid.c, and the
          # pq-crystals reference these follow).  C leaves it
          # implementation-defined; every supported target implements it as an
          # arithmetic shift, and the unsigned-cast rewrite would obscure the
          # constant-time intent at 12 sites for no behavioural change.
          #
          # uninitvar: false positives on out-parameters.  cppcheck cannot see
          # through the helpers that fill them.  Five sites (six reports: `src/c/ama_kyber.c:572` is emitted once per
          # configuration `--force` invents), listed WITH their
          # line numbers and their reasons in .cppcheck-suppressions.
          #
          # They used to be three file-wide `--suppress=uninitvar:<file>`
          # entries, and "Scoped per file so the check stays live elsewhere"
          # undersold what that cost: a genuinely uninitialised read introduced
          # later anywhere in ama_nistp.c — 1,100 lines of P-256/P-384/P-521
          # field arithmetic — was reported and discarded.  Measured by
          # injecting one:
          #
          #   file-wide suppression : 0 reports
          #   per-site suppressions : src/c/ama_nistp.c:469:31: error:
          #                           Uninitialized variable: ama_probe_uninit
          #
          # arrayIndexOutOfBounds: NOT suppressed any more, because it was
          # never a cppcheck limitation.  With --force, cppcheck invents a
          # configuration where PATH_MAX is defined but unknown, evaluates
          # `char dir[AMA_DISPATCH_PATH_MAX]` as `dir[1]`, and reports the
          # writes into it.  Passing the real value with -DPATH_MAX=4096 makes
          # both findings disappear AND leaves the bounds check live on every
          # `char[AMA_DISPATCH_PATH_MAX]` buffer in the file — which, being a
          # file full of them, is the one place the suppression cost most.
          cppcheck \
            --enable=warning,performance,portability \
            --suppress=missingIncludeSystem \
            --suppress=unusedFunction \
            --suppress=shiftTooManyBitsSigned \
            --suppressions-list=.cppcheck-suppressions \
            --error-exitcode=1 \
            --inline-suppr \
            --std=c11 \
            -I include/ \
            -DAMA_USE_NATIVE_PQC \
            -DPATH_MAX=4096 \
            --force \
            src/c/ 2>&1 | tee cppcheck-report.txt

      - name: Upload cppcheck report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: cppcheck-report
          path: cppcheck-report.txt
          retention-days: 30

  clang-analyzer:
    name: Clang Static Analyzer (scan-build)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install dependencies
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list /etc/apt/sources.list.d/azure-cli.list || true
          .github/scripts/apt-install.sh cmake clang clang-tools

      - name: Run scan-build
        run: |
          mkdir -p build-scan
          cd build-scan
          scan-build --status-bugs \
            -enable-checker security.FloatLoopCounter \
            -enable-checker security.insecureAPI.UncheckedReturn \
            -enable-checker alpha.security.ArrayBoundV2 \
            -enable-checker alpha.security.MallocOverflow \
            -enable-checker alpha.security.ReturnPtrRange \
            -enable-checker alpha.security.taint.TaintPropagation \
            cmake .. \
              -DCMAKE_BUILD_TYPE=None \
              -DCMAKE_C_COMPILER=clang \
              -DAMA_USE_NATIVE_PQC=ON \
              -DAMA_BUILD_TESTS=OFF \
              -DAMA_BUILD_EXAMPLES=OFF \
              -DAMA_ENABLE_LTO=OFF
          scan-build --status-bugs \
            -enable-checker security.FloatLoopCounter \
            -enable-checker security.insecureAPI.UncheckedReturn \
            -enable-checker alpha.security.ArrayBoundV2 \
            -enable-checker alpha.security.MallocOverflow \
            -enable-checker alpha.security.ReturnPtrRange \
            -enable-checker alpha.security.taint.TaintPropagation \
            make -j$(nproc)

      - name: Upload scan-build report
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: scan-build-report
          path: /tmp/scan-build-*
          retention-days: 30

  codeql:
    name: CodeQL Security Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # The one job that uploads SARIF carries the one write grant the
    # workflow used to hand to all of its jobs.
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Initialize CodeQL
        uses: github/codeql-action/init@e4fba868fa4b1b91e1fdab776edc8cfbe6e9fb81  # v4.37.3
        with:
          languages: c-cpp, python
          queries: security-and-quality
          config-file: ./.github/codeql/codeql-config.yml

      - name: Build C library for CodeQL
        run: |
          # None: CodeQL and the clang static analyzer read the source the
          # compiler is given, not the code it emits, so the optimizer plays no
          # part.  Stated rather than defaulted.
          cmake -B build \
            -DCMAKE_BUILD_TYPE=None \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_BUILD_TESTS=OFF \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_ENABLE_LTO=OFF
          cmake --build build -j$(nproc)

      - name: Perform CodeQL Analysis
        # No explicit category: this job initializes TWO languages, and the
        # action derives a distinct per-language category for each SARIF it
        # uploads when none is pinned.  The previous
        # `category: "/language:c-cpp"` stamped the python run's upload with
        # the c-cpp label as well, so both languages' alerts filed under one
        # analysis identity.
        uses: github/codeql-action/analyze@e4fba868fa4b1b91e1fdab776edc8cfbe6e9fb81  # v4.37.3

  compiler-warnings:
    name: Strict Compiler Warnings
    runs-on: ubuntu-latest
    # Two full builds per compiler now (unoptimized and Release+LTO), so the
    # 10-minute budget that covered one no longer fits.
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, clang]
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install dependencies
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list /etc/apt/sources.list.d/azure-cli.list || true
          .github/scripts/apt-install.sh cmake ${{ matrix.compiler }}

      # This step was named "Build with strict warnings (Werror)" and did not
      # pass -Werror. Nor did CMakeLists.txt. It compiled with an expanded
      # warning set, printed 68 warnings under gcc and 69 under clang, and
      # exited 0 — a job that could not fail on the thing it is named after,
      # which is the same shape as every other gate defect this release fixed.
      #
      # -Werror over the whole set is not achievable here and pretending
      # otherwise would just produce a permanently-disabled job. So the
      # categories that ARE achievable are made fatal, and they were first
      # driven to zero at source rather than suppressed:
      #
      #   -Werror=missing-prototypes  Every one of these was a symbol declared
      #                               by `extern` in each consumer separately —
      #                               the MULX+ADX asm kernels, the FROST and
      #                               Kyber test-only exports, the renamed
      #                               X25519 ladders. Three independent
      #                               transcriptions of one signature, on raw
      #                               uint64_t[4]/uint8_t[32] buffers, where a
      #                               mismatch is silent rather than a compile
      #                               error. Each now has one shared internal
      #                               header (src/c/internal/*.h,
      #                               tests/c/x25519_equiv_ladders.h).
      #   -Werror=shadow              Two `rc` and one `pub33` shadowed in the
      #                               C tests.
      #   -Werror=unused-function     Dead DRBG hook installers in test_kat.c,
      #                               deleted; and fe64.h's frombytes/tobytes
      #                               were plain `static` in a header where
      #                               every sibling and every fe51.h
      #                               counterpart is `static inline`.
      #
      # An earlier revision of this comment said -Wconversion and clang's
      # -Wimplicit-int-conversion were "concentrated in the Kyber arithmetic"
      # and left un-attempted next to a release. That inventory has since
      # been driven to zero at source with per-site range proofs, not blanket
      # casts: barrett_reduce (ama_kyber.c and the avx2 scalar twin) keeps all
      # intermediates in int32 with the quotient exhaustively verified in
      # [-10, 9] over every int16_t input, and the d=4/d=5 packers plus the
      # d=11 unpacker carry the same explicit casts the d=10 branch always
      # had (the FIPS 203 KATs pin the serialization bit-for-bit).
      #
      # What legitimately remains is exactly two documented language-extension
      # classes, both required by construction:
      #   fe51.h / fe64.h            "ISO C does not support '__int128'"
      #                              (-Wpedantic, gcc) — the 128-bit limbs the
      #                              field arithmetic is built on.
      #   x86/ama_nistp_mont_mulx.c  overlength string literal (-Woverlength-
      #                              strings, clang) — one atomic asm() block;
      #                              splitting the Montgomery kernel into
      #                              several asm statements would forfeit the
      #                              register-state guarantees the kernel
      #                              depends on, and the warning measures the
      #                              concatenated literal anyway.
      # The "Enforce the frozen warning allowlist" step below fails this job
      # if ANY other warning appears, so the clean state is now a gate, not a
      # snapshot.
      #
      # TWO configurations are built, because one of them could not see an
      # entire class of diagnostic.  The original step passed no
      # CMAKE_BUILD_TYPE at all, so it compiled at -O0 — and
      # -Wmaybe-uninitialized, -Wstringop-truncation, -Wstringop-overflow,
      # -Wrestrict and everything _FORTIFY_SOURCE derives are computed from
      # dataflow the optimizer builds and are simply not emitted there.
      # Measured when this second lane was added: the same tree that produced
      # ZERO warnings at -O0 produced seven -Wstringop-truncation in
      # benchmarks/benchmark_c_raw.c at Release, and one
      # "'pk_k' may be used uninitialized" with LTO on — the configuration
      # `make c` and the release wheels actually use.  A gate whose green
      # describes a configuration nobody ships is a snapshot again.
      - name: Build with strict warnings (unoptimized)
        shell: bash
        # pipefail is load-bearing: without it the pipeline's status is
        # tee's (always 0) and a compile error could not fail this step —
        # the same defect the cppcheck job above documents.
        env:
          # GCC quotes identifiers in diagnostics with U+2018/U+2019 when the
          # locale is UTF-8 (which GitHub runners set: LANG=C.UTF-8) and with
          # ASCII apostrophes otherwise.  The allowlist matches on that text,
          # so the text must not depend on the runner's locale.  Pinning
          # LC_ALL here makes the diagnostics byte-reproducible between a
          # developer's shell and CI; the allowlist patterns in
          # tools/check_compiler_warnings.py are *also* quote-agnostic so a
          # future locale change cannot silently break the gate the way it did
          # at 94e4bd6.
          LC_ALL: C
        run: |
          set -o pipefail
          # CMAKE_BUILD_TYPE=None states what this configure means: the
          # UNOPTIMIZED half of the pair (the step below is the Release+LTO
          # half), which is what makes the two logs a two-configuration
          # warning sweep.  Stated explicitly because "no build type" and
          # "forgot the build type" are otherwise the same text, and this
          # project defaults neither.
          cmake -B build-strict \
            -DCMAKE_BUILD_TYPE=None \
            -DCMAKE_C_COMPILER=${{ matrix.compiler }} \
            -DCMAKE_C_FLAGS="-Wall -Wextra -Wpedantic -Wshadow -Wformat=2 -Wconversion -Wno-sign-conversion \
                             -Werror=missing-prototypes -Werror=shadow -Werror=unused-function" \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_BUILD_TESTS=ON \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_ENABLE_LTO=OFF
          # -Otarget: without it, two compiler processes under -j N share one
          # pipe and their stderr can interleave INSIDE a single line — observed
          # in a clean parallel build of this tree, where two identical
          # -Woverlength-strings diagnostics merged into one unparseable line.
          # An allowlisted warning that no longer matches its pattern turns this
          # gate red for a reason unrelated to the code.  Make serialises
          # per-target output with -Otarget; Ninja already buffers per edge.
          cmake --build build-strict -j$(nproc) -- -Otarget 2>&1 | tee build-warnings.log

      - name: Build with strict warnings (Release + LTO, the shipped configuration)
        shell: bash
        env:
          LC_ALL: C
        run: |
          set -o pipefail
          # AMA_ENABLE_NATIVE_ARCH stays OFF explicitly: -march=native would
          # make the diagnostic set depend on whichever runner SKU picked up
          # the job, which is the opposite of a frozen allowlist.
          cmake -B build-strict-release \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER=${{ matrix.compiler }} \
            -DCMAKE_C_FLAGS="-Wall -Wextra -Wpedantic -Wshadow -Wformat=2 -Wconversion -Wno-sign-conversion \
                             -Werror=missing-prototypes -Werror=shadow -Werror=unused-function" \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_BUILD_TESTS=ON \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_ENABLE_NATIVE_ARCH=OFF \
            -DAMA_ENABLE_LTO=ON
          cmake --build build-strict-release -j$(nproc) -- -Otarget 2>&1 | tee build-warnings-release.log

      - name: Enforce the frozen warning allowlist
        # The allowlist itself now lives in tools/check_compiler_warnings.py
        # rather than in a chain of `grep -v` here, for one reason: it has to
        # be applied to more than one log.  Three build configurations across
        # two jobs (unoptimized, Release+LTO, and the AArch64 cross-build
        # below) each need the identical decision, and three transcriptions of
        # an allowlist is how a gate ends up enforcing three different things.
        # The script is covered by tests/test_compiler_warning_gate.py, which
        # pins that it fails on a real out-of-allowlist warning and that a
        # missing or empty log is fatal rather than vacuously clean.
        shell: bash
        run: |
          python3 tools/check_compiler_warnings.py \
            build-warnings.log build-warnings-release.log

      - name: Run tests
        run: cd build-strict && ctest --output-on-failure

  compiler-warnings-aarch64:
    name: Strict Compiler Warnings (AArch64 cross, NEON + SVE2)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # The x86-64 job above cannot see src/c/neon/** or src/c/sve2/** at all:
    # on x86 both collapse to a placeholder typedef in their `#else` branch.
    # When this job was first run against the branch it reported 36
    # -Wmissing-prototypes — a class the strict flag set makes FATAL — because
    # every NEON and SVE2 kernel was defined with no visible declaration while
    # src/c/dispatch/ama_dispatch.c, src/c/ama_sha256.c and
    # tests/c/test_sha256_neon_kat.c each re-transcribed the signatures by
    # hand.  Those are raw buffer signatures whose addresses land in a
    # function-pointer table, so drift between a transcription and its
    # definition is undefined behaviour at the indirect call, not a compile
    # error.  Fixed at source by src/c/neon/ama_neon_internal.h and
    # src/c/sve2/ama_sve2_internal.h; this job is what keeps it fixed.
    #
    # SVE2 is built in the same job because AMA_ENABLE_SVE2 defaults OFF, so
    # those eleven kernels are doubly invisible: wrong architecture AND off by
    # default.  No tests are run here — correctness on AArch64 is
    # arm-qemu.yml's job; this one is about the diagnostics.
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install AArch64 cross toolchain
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list /etc/apt/sources.list.d/azure-cli.list || true
          .github/scripts/apt-install.sh --no-install-recommends cmake ninja-build gcc-aarch64-linux-gnu g++-aarch64-linux-gnu libc6-dev-arm64-cross

      - name: Build with strict warnings (AArch64, NEON)
        shell: bash
        env:
          LC_ALL: C
        run: |
          set -o pipefail
          cmake -S . -B build-strict-arm -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE=cmake/toolchains/aarch64-linux-gnu.cmake \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_FLAGS="-Wall -Wextra -Wpedantic -Wshadow -Wformat=2 -Wconversion -Wno-sign-conversion \
                             -Werror=missing-prototypes -Werror=shadow -Werror=unused-function" \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_AES_CONSTTIME=ON \
            -DAMA_BUILD_TESTS=ON \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_BUILD_FUZZ=OFF \
            -DAMA_ENABLE_NATIVE_ARCH=OFF \
            -DAMA_ENABLE_LTO=OFF
          cmake --build build-strict-arm -j$(nproc) 2>&1 | tee build-warnings-arm.log

      - name: Build with strict warnings (AArch64, SVE2 kernels enabled)
        shell: bash
        env:
          LC_ALL: C
        run: |
          set -o pipefail
          cmake -S . -B build-strict-arm-sve2 -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE=cmake/toolchains/aarch64-linux-gnu.cmake \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_FLAGS="-Wall -Wextra -Wpedantic -Wshadow -Wformat=2 -Wconversion -Wno-sign-conversion \
                             -Werror=missing-prototypes -Werror=shadow -Werror=unused-function" \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_AES_CONSTTIME=ON \
            -DAMA_ENABLE_SVE2=ON \
            -DAMA_BUILD_TESTS=ON \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_BUILD_FUZZ=OFF \
            -DAMA_ENABLE_NATIVE_ARCH=OFF \
            -DAMA_ENABLE_LTO=OFF
          cmake --build build-strict-arm-sve2 -j$(nproc) 2>&1 | tee build-warnings-arm-sve2.log

      - name: Enforce the frozen warning allowlist
        shell: bash
        run: |
          python3 tools/check_compiler_warnings.py \
            build-warnings-arm.log build-warnings-arm-sve2.log

  version-consistency:
    # Enforces audit 5a: every file that declares the library version must
    # agree with ama_cryptography/__init__.py. Also enforces audit 6a:
    # root INVARIANTS.md is canonical, while .github/INVARIANTS.md must
    # remain a short pointer so a second divergent copy cannot reappear.
    name: Version / Invariants Consistency
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Run version consistency check
        run: python3 tools/check_version_consistency.py

  address-sanitizer:
    name: AddressSanitizer + UBSan
    runs-on: ubuntu-latest
    # 25, not 15.  This job runs `ctest --output-on-failure` over the WHOLE C
    # suite under ASan+UBSan, which is the same command `memory-sanitizer`
    # below runs under a HEAVIER sanitizer with a 25-minute budget — the two
    # disagreeing about the cost of an identical workload was the defect, the
    # same way the three dudect jobs disagreeing (300/600/900 s) was.
    #
    # It was not theoretical: on 2456cdd this job was cancelled at its cap,
    # 15m13s in, still actively running ctest inside `test_sphincs_simd_equiv`
    # — SLH-DSA equivalence, which is expensive natively and much more so under
    # ASan.  Nothing hung; the budget simply could not cover the work.  A
    # cancelled dependency is not a success, so `Static Analysis Gate` went red
    # on a commit where cppcheck, clang-tidy, scan-build, all three
    # strict-warning jobs, version-consistency, the MSan KAT lane and the
    # reproducible-build check had every one passed.
    timeout-minutes: 25
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install dependencies
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list /etc/apt/sources.list.d/azure-cli.list || true
          .github/scripts/apt-install.sh cmake clang

      - name: Build with ASan + UBSan
        # -O1 matches the MemorySanitizer, ThreadSanitizer and Valgrind jobs in
        # this file, and it is the root cause of the timeout this job hit at
        # 15m13s inside test_sphincs_simd_equiv.  This project sets no default
        # CMAKE_BUILD_TYPE, so without an -O flag in CMAKE_C_FLAGS the library
        # compiled unoptimized while MSan ran the IDENTICAL ctest at -O1 under a
        # heavier sanitizer.  Raising the cap treated the symptom; the two jobs
        # were not running the same workload.
        run: |
          cmake -B build-asan \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_C_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer -g -O1" \
            -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined" \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_BUILD_TESTS=ON \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_ENABLE_LTO=OFF
          cmake --build build-asan -j$(nproc)

      - name: Run tests under sanitizers
        env:
          ASAN_OPTIONS: detect_leaks=1:detect_stack_use_after_return=1
          UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1
        run: cd build-asan && ctest --output-on-failure

  # =========================================================================
  # Extended sanitizer + clang-tidy + reproducible-build matrix
  # (audit Issues 9 + 10) — runs weekly on the schedule above plus on
  # manual workflow_dispatch.  PR-time and push-time runs only invoke
  # the original ASan+UBSan gate above; this matrix is intentionally
  # out of the merge critical path because MSan/TSan/clang-tidy/
  # reproducible-build can each take 15-25 minutes by themselves.
  # =========================================================================

  memory-sanitizer:
    name: MemorySanitizer (uninit reads)
    # MSan needs every linked library — including libc++ / libstdc++ —
    # to be MSan-instrumented or the false-positive volume becomes
    # unmanageable.  We use clang's `-stdlib=libc++` with the
    # pre-instrumented runtime that ships with clang-18.  This is a
    # known cost of MSan that the project accepts because the
    # uninitialized-read class is invisible to ASan and the
    # constant-time codebase has hot spots (lookup masks, conditional
    # selects) that an uninit-read can mask without symptom.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install dependencies
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list /etc/apt/sources.list.d/azure-cli.list || true
          .github/scripts/apt-install.sh cmake clang-18 libc++-18-dev libc++abi-18-dev

      - name: Build with MSan
        run: |
          cmake -B build-msan \
            -DCMAKE_C_COMPILER=clang-18 \
            -DCMAKE_CXX_COMPILER=clang++-18 \
            -DCMAKE_C_FLAGS="-fsanitize=memory -fsanitize-memory-track-origins=2 -fno-omit-frame-pointer -g -O1" \
            -DCMAKE_CXX_FLAGS="-fsanitize=memory -fsanitize-memory-track-origins=2 -fno-omit-frame-pointer -g -O1 -stdlib=libc++" \
            -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=memory -stdlib=libc++" \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_BUILD_TESTS=ON \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_ENABLE_LTO=OFF \
            -DAMA_ENABLE_SIMD=OFF \
            -DAMA_AES_CONSTTIME=ON
          cmake --build build-msan -j$(nproc)

      - name: Run tests under MSan
        env:
          MSAN_OPTIONS: print_stacktrace=1:halt_on_error=1:abort_on_error=1
        # No `-L fast` label exists in tests/c/CMakeLists.txt today
        # (Copilot review #322).  CTest exits successfully when zero
        # tests match a label, so the previous `ctest -L fast ||
        # fallback` construction would have passed without running
        # anything.  Run the full suite directly until a `fast` label
        # taxonomy is introduced.  Failures surface in the usual way.
        run: cd build-msan && ctest --output-on-failure

  thread-sanitizer:
    name: ThreadSanitizer (dispatch init races)
    # TSan targets the multi-threaded code in ama_cpuid.c +
    # ama_dispatch.c — the platform once-primitive (pthread_once on
    # POSIX, InitOnceExecuteOnce on Windows) per INVARIANT-15.  A
    # data race on the dispatch table would cause SIMD kernels to be
    # called with NULL function pointers from one thread while another
    # is still initialising; TSan is the only sanitiser that detects
    # this class.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install dependencies
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list /etc/apt/sources.list.d/azure-cli.list || true
          .github/scripts/apt-install.sh cmake clang

      - name: Build with TSan
        run: |
          cmake -B build-tsan \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_C_FLAGS="-fsanitize=thread -fno-omit-frame-pointer -g -O1" \
            -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=thread" \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_BUILD_TESTS=ON \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_ENABLE_LTO=OFF \
            -DAMA_AES_CONSTTIME=ON
          cmake --build build-tsan -j$(nproc)

      - name: Run tests under TSan
        env:
          TSAN_OPTIONS: halt_on_error=1:second_deadlock_stack=1
        run: cd build-tsan && ctest --output-on-failure

      # Non-vacuity, the same shape the Valgrind lane above uses for its
      # target count.  TSan reports a race only when two threads touch the
      # same location at the same time, so this lane is worth exactly as much
      # as the concurrency in the suite it runs.  For most of this project's
      # history that concurrency was zero: of all the C tests exactly one
      # created a thread, and it joined on the next statement, so the job
      # instrumented a single-threaded program and could not report the class
      # its own comment above names.  A live race in the shipped library
      # (nistp_mulx_gate, src/c/ama_nistp.c) sat under this green lane.
      # tests/c/test_concurrent_init.c is what the lane observes; if it stops
      # running, the lane is decorative again and must fail rather than pass.
      - name: Assert the lane had concurrency to observe
        run: |
          cd build-tsan
          if ! ctest -N | grep -q 'Test *#[0-9]*: *test_concurrent_init'; then
            echo "::error::test_concurrent_init is not registered — the TSan lane"
            echo "::error::has no concurrent test to observe and cannot fail on a race."
            exit 1
          fi
          echo "OK  test_concurrent_init is registered; the lane is non-vacuous"

  valgrind-memcheck:
    name: Valgrind memcheck (defense-in-depth)
    # Complements ASan with a different leak/uninit reader.  Slow —
    # ~5x runtime — so scheduled only.  This lane runs a FOCUSED subset
    # (the six most coverage-rich binaries listed in the run step), not the
    # full ctest suite the ASan job runs — Valgrind over the whole suite would
    # exceed the runner timeout.  Its strength is catching uninit reads ASan
    # misses (different implementation strategy) and giving a second opinion on
    # the ASan reports for free.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install dependencies
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list /etc/apt/sources.list.d/azure-cli.list || true
          .github/scripts/apt-install.sh cmake clang valgrind

      - name: Build (no sanitizers, just symbols + no LTO)
        run: |
          cmake -B build-valgrind \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_C_FLAGS="-g -O1 -fno-omit-frame-pointer" \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_BUILD_TESTS=ON \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_ENABLE_LTO=OFF \
            -DAMA_AES_CONSTTIME=ON \
            -DAMA_ENABLE_SIMD=OFF
          cmake --build build-valgrind -j$(nproc)

      - name: Run a focused subset under Valgrind memcheck
        run: |
          set -euo pipefail
          cd build-valgrind
          # Run the most coverage-rich subset under Valgrind — the
          # PQC primitives and AEAD tests exercise the largest stack
          # / heap surface.  Running the full ctest suite under
          # Valgrind would exceed the runner timeout.
          tests="bin/test_consttime bin/test_core bin/test_aes_gcm_scalar_kat bin/test_chacha20poly1305 bin/test_ed25519 bin/test_sha3"
          expected=6
          ran=0
          for test in $tests; do
            if [ -x "$test" ]; then
              echo "=== valgrind: $test ==="
              valgrind \
                --error-exitcode=1 \
                --leak-check=full \
                --show-leak-kinds=definite,indirect \
                --errors-for-leak-kinds=definite \
                --track-origins=yes \
                "$test"
              ran=$((ran + 1))
            else
              echo "::error::Valgrind target not found or not executable: $test"
            fi
          done
          # Non-vacuity floor: a renamed target or a moved bin/ would otherwise
          # let this lane pass with zero invocations while the row still read
          # "6/6" (audit Low).  Require every expected binary to have run.
          if [ "$ran" -ne "$expected" ]; then
            echo "::error::Valgrind ran $ran/$expected targets — refusing to report success on a partial run."
            exit 1
          fi
          echo "Valgrind memcheck clean across all $ran/$expected targets."

  clang-tidy:
    name: clang-tidy (security + correctness, fail-closed)
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # FAIL-CLOSED (audit Issue 9 close-out).  The codebase is
    # clang-tidy-clean against the enabled check inventory in
    # `.clang-tidy`; `WarningsAsErrors: '*'` there flips every finding
    # to a hard error.  A new finding fails this job, which fails the
    # workflow, which fails the PR.  See `.clang-tidy` header for the
    # three checks dropped from the enabled list with rationale (each
    # incompatible with the project's cryptographic-C style — dropped
    # explicitly, not silenced per-site).  The previous advisory
    # posture (continue-on-error + trailing exit 0) was removed in
    # this commit.
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install dependencies
        run: |
          sudo rm -f /etc/apt/sources.list.d/microsoft-prod.list /etc/apt/sources.list.d/azure-cli.list || true
          .github/scripts/apt-install.sh cmake clang clang-tidy

      - name: Configure and generate compile_commands.json
        run: |
          # None: clang-tidy consumes compile_commands.json, which records the
          # compiler invocation and not its output.
          cmake -B build-tidy \
            -DCMAKE_BUILD_TYPE=None \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_BUILD_TESTS=OFF \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_ENABLE_LTO=OFF \
            -DAMA_AES_CONSTTIME=ON

      - name: Run clang-tidy on our C sources (fail-closed)
        # Only run on src/c/ and include/ (every C source is first-party;
        # there is no vendored tree).  The .clang-tidy config file at repo root
        # drives check selection AND the WarningsAsErrors flip; CI
        # just enumerates the input files and propagates the exit
        # status.  Output is captured to an artefact regardless of
        # status so reviewers can scan findings without re-running
        # locally.
        #
        # FAIL-CLOSED semantics (audit Issue 9 close-out): no
        # `continue-on-error`, no `set +e`, no `exit 0`.  `clang-tidy`
        # exits non-zero when it emits any error (which it now does
        # for every enabled check, per `.clang-tidy::WarningsAsErrors`),
        # and that exit status fails this step.
        #
        # `set -o pipefail` IS load-bearing here (PR #326 follow-up):
        # without it the inner `if ! clang-tidy ... | tee ...` only
        # sees `tee`'s exit code (always 0), so any clang-tidy error
        # is silently swallowed and `rc` never flips off 0 — which
        # is how the pre-existing `cert-err34-c` findings in
        # `dispatch_cache_load()` (atoi() calls introduced in
        # `58e7a2d`) were passing CI even under this section's
        # "FAIL-CLOSED" claim.  Now genuinely fail-closed.
        shell: bash
        run: |
          set -o pipefail
          files=$(find src/c include \
            -type f \( -name '*.c' -o -name '*.h' \) -print)
          if [ -z "$files" ]; then
            echo "ERROR: no C/H source files found — is the layout intact?"
            exit 1
          fi
          # `--quiet` suppresses the "X warnings generated" per-file
          # banner; diagnostics still print.  Serial run so output is
          # ordered; tidy itself is single-threaded per TU.
          : > clang-tidy-findings.txt
          rc=0
          while IFS= read -r f; do
            if ! clang-tidy -p build-tidy --quiet "$f" 2>&1 | tee -a clang-tidy-findings.txt; then
              rc=1
            fi
          done <<< "$files"
          if [ "$rc" -ne 0 ]; then
            echo "::error::clang-tidy found errors — see uploaded clang-tidy-findings artefact."
          fi
          exit "$rc"

      - name: Upload clang-tidy findings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: clang-tidy-findings
          path: clang-tidy-findings.txt
          retention-days: 30

  reproducible-build:
    name: Reproducible Build (digest + .py + native artefact byte-equality)
    # INVARIANT-8 declares deterministic builds.  Audit Issue 10
    # close-out: in addition to the STRICT digest + .py check that
    # PR #322 introduced, this job now ALSO strictly diffs the
    # compiled native artefacts (libama_cryptography.so / Cython
    # extension .so) between the two passes.
    #
    # SCOPE OF THIS JOB:
    #
    #   STRICT  — INTEGRITY_DIGEST_HEX in _integrity_signature.py must
    #             match across both builds.  Artefact whose stability
    #             the import-time integrity gate depends on.
    #   STRICT  — Every .py file inside the wheel must be byte-identical
    #             across both builds (excluding the per-build ephemeral
    #             `_integrity_signature.py` — INVARIANT-17 keeps that
    #             file non-byte-stable; the digest check above
    #             validates the OTHER .py files that the digest is
    #             computed over).
    #   STRICT  — Native artefacts (.so / .pyd / Cython-built .so)
    #             must be byte-identical across both builds.
    #             Promoted from ADVISORY in the audit Issue 10
    #             close-out.  Achieved by:
    #               1. Pinned manylinux_2_28 container — locks the
    #                  gcc / glibc / binutils / cmake versions so the
    #                  host toolchain cannot drift between two CI runs.
    #               2. -fdebug-prefix-map=$PWD=. strips host paths
    #                  from DWARF debug info (otherwise the path
    #                  difference between two checkout dirs leaks into
    #                  the .so).
    #               3. -Wl,--build-id=sha1 forces the linker's build-id
    #                  to a content-derived hash instead of the default
    #                  "random" mode that picks a fresh build-id per
    #                  invocation.
    #               4. Deterministic `ar` archive members (no
    #                  timestamps / UID / GID).  This comes from
    #                  binutils' `--enable-deterministic-archives`
    #                  default, NOT from an `AR_FLAGS`/`ARFLAGS` env
    #                  var: CMake invokes `<CMAKE_AR> qc` directly and
    #                  does not read those (see the AR_FLAGS note below
    #                  the container block).  A future non-deterministic
    #                  `ar` is caught by the strict native-artefact diff
    #                  below.
    #
    # The signature artefact `_integrity_signature.py` legitimately
    # differs between builds (INVARIANT-17 ephemeral per-build keypair).
    # The wheel `RECORD` file is regenerated by pip with hashes of all
    # OTHER files, so its byte-equality follows transitively from the
    # rest — but it pins a relative path that pip canonicalises, so
    # for the native-artefact diff we exclude RECORD explicitly and
    # rely on the .py check above to catch any drift in inputs.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    container:
      # PINNED manylinux_2_28 image (audit Issue 10 close-out).  This
      # is the toolchain anchor that makes native-artefact byte-equality
      # achievable on a public CI runner.  The tag is the date-stamped
      # form `YYYY.MM.DD-N` (NOT `:latest`, NOT the floating
      # `:manylinux_2_28` rolling tag) so this gate stays stable across
      # the manylinux project's rolling updates.  When a tag bump is
      # needed (e.g., the pinned tag is yanked from the registry), do
      # it as its own commit so the reproducible-build delta is
      # auditable in isolation.  Promoting to a `@sha256:` digest pin
      # is a follow-up — INVARIANT-8 addendum.
      #
      # Tag verified against
      # https://quay.io/api/v1/repository/pypa/manylinux_2_28_x86_64/tag/
      # at PR #323 ready-for-review time (2026-05-20).  The previous
      # fabricated tag `:2026-04-21-f5ec593` did not exist and the
      # docker pull failed with "manifest not found" — Copilot review
      # #323 follow-up (CI failure on first run).
      image: quay.io/pypa/manylinux_2_28_x86_64:2026.05.17-1
    timeout-minutes: 30
    # AR_FLAGS=Drcs was set here in the audit Issue 10 close-out
    # commit but turned out to be a no-op: CMake's archive creation
    # invokes `<CMAKE_AR> qc <TARGET> ...` directly and does NOT
    # honour the GNU-make `AR_FLAGS` (or `ARFLAGS`) env var
    # (Copilot review #323 round 2).  Modern binutils' `ar` defaults
    # to deterministic mode anyway (the `--enable-deterministic-archives`
    # configure-time default since binutils 2.27, March 2016), so
    # archive metadata determinism is implicit on every reasonable
    # toolchain.  If a future regression brings back a non-deterministic
    # `ar`, the strict native-artefact diff below will catch it and
    # the fix is to use `CMAKE_C_ARCHIVE_CREATE` overrides, not an
    # env var the build doesn't read.
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install build prerequisites (manylinux_2_28 / AlmaLinux 8)
        run: |
          # The manylinux_2_28 image carries gcc-toolset, cmake, and
          # CPython 3.10–3.13 under /opt/python/.  Pre-install every
          # build dep into the container's CPython 3.11 — `build` /
          # `setuptools` / `wheel` / `cmake` / `Cython` / `numpy` —
          # so the wheel build can run with `--no-isolation` below.
          #
          # WHY `--no-isolation`: PEP 517 build isolation creates a
          # fresh `/tmp/build-env-<random8>/` directory per invocation
          # and stages build deps inside it.  When Cython compiles
          # `.pyx -> .c -> .so`, the NumPy include path (and other
          # header paths) inside that random directory get expanded
          # via `__FILE__` macros into rodata strings inside the
          # resulting native artefact.  Two builds produce two
          # different random suffixes → two different rodata strings
          # → byte-different `.so` (and a downstream
          # `--build-id=sha1` mismatch because the build-ID is a
          # hash of section content).  `--no-isolation` skips the
          # random isolation dir entirely; build deps come from the
          # container's pre-installed CPython site-packages, which
          # is the same on every run.  This is what the manylinux
          # image is designed for.
          /opt/python/cp311-cp311/bin/python -m pip install --upgrade pip
          /opt/python/cp311-cp311/bin/pip install \
            'build>=1.0' \
            'setuptools>=83.0.0' 'wheel>=0.47.0' \
            'cmake>=4.4.0' 'Cython>=3.2.8' 'numpy>=1.24.0'
          # Front-load the manylinux Python on PATH so subsequent
          # steps see `python` / `pip` as the pinned 3.11 from the
          # base image (and not a stray host Python that GitHub's
          # ubuntu host might have leaked through into the container).
          echo "/opt/python/cp311-cp311/bin" >> "$GITHUB_PATH"

      - name: Build wheel — pass 1
        env:
          AMA_BUILD_PIPELINE: "1"
          SOURCE_DATE_EPOCH: "1700000000"   # 2023-11-14 — stable reference epoch
          PYTHONHASHSEED: "0"
          PYTHONDONTWRITEBYTECODE: "1"
          # CFLAGS — three overlapping prefix-map flags so every
          # variant of host-path leakage gets neutralised inside
          # the workspace tree:
          #   -fdebug-prefix-map  : strips host paths from DWARF
          #                          debug-info.
          #   -ffile-prefix-map   : superset that ALSO covers __FILE__
          #                          macro expansion (some C code
          #                          embeds __FILE__ for logging).
          #   -fmacro-prefix-map  : the macro half of file-prefix-map,
          #                          explicit for older GCC that split
          #                          the two flags.
          # The PEP 517 isolation dir is handled separately via the
          # `--no-isolation` flag on `python -m build` below — see
          # the install step's WHY paragraph for the full rationale.
          # LDFLAGS — `--build-id=sha1` makes the build-id a content
          # hash instead of a fresh random value per link.  Combined
          # with deterministic section content, this produces an
          # identical build-id across two passes.
          # `MAKEFLAGS=-j1` and `CMAKE_BUILD_PARALLEL_LEVEL=1` force
          # sequential compilation so parallel-build write-order
          # variation cannot leak into the .so.
          # Use ${{ github.workspace }} (expanded by Actions) rather than the
          # POSIX ${GITHUB_WORKSPACE}, which is NOT expanded inside an env: map
          # and would reach the compiler as a literal string that never matches
          # the checkout path — silently disabling all three prefix-map flags.
          CFLAGS: "-fdebug-prefix-map=${{ github.workspace }}=. -ffile-prefix-map=${{ github.workspace }}=. -fmacro-prefix-map=${{ github.workspace }}=."
          LDFLAGS: "-Wl,--build-id=sha1"
          MAKEFLAGS: "-j1"
          CMAKE_BUILD_PARALLEL_LEVEL: "1"
        run: |
          rm -rf build dist
          # `--no-isolation`: see Install step's WHY paragraph.  The
          # build deps are pre-installed into the container Python in
          # the previous step, so the wheel build can run against
          # them directly without staging into /tmp/build-env-<random>.
          python -m build --wheel --no-isolation --outdir dist1/
          ls -lh dist1/

      - name: Build wheel — pass 2 (independent rebuild)
        env:
          AMA_BUILD_PIPELINE: "1"
          SOURCE_DATE_EPOCH: "1700000000"
          PYTHONHASHSEED: "0"
          PYTHONDONTWRITEBYTECODE: "1"
          # Use ${{ github.workspace }} (expanded by Actions) rather than the
          # POSIX ${GITHUB_WORKSPACE}, which is NOT expanded inside an env: map
          # and would reach the compiler as a literal string that never matches
          # the checkout path — silently disabling all three prefix-map flags.
          CFLAGS: "-fdebug-prefix-map=${{ github.workspace }}=. -ffile-prefix-map=${{ github.workspace }}=. -fmacro-prefix-map=${{ github.workspace }}=."
          LDFLAGS: "-Wl,--build-id=sha1"
          MAKEFLAGS: "-j1"
          CMAKE_BUILD_PARALLEL_LEVEL: "1"
        run: |
          rm -rf build
          python -m build --wheel --no-isolation --outdir dist2/
          ls -lh dist2/

      - name: Compare integrity digest + .py content across builds
        run: |
          # Extract bytewise contents from each wheel and assert
          # equality on the load-bearing contract surface.
          #
          # IMPORTANT subtlety (INVARIANT-17 / Copilot review #322
          # follow-up): `ama_cryptography/_integrity_signature.py` is
          # NOT byte-stable across builds, and that is deliberate:
          # `_build_sign.py` generates an *ephemeral* Ed25519 keypair
          # per build, signs the SHA3-256 digest of the OTHER .py
          # files with it, and writes the (public key, signature) pair
          # into `_integrity_signature.py`.  The private key is
          # discarded immediately.  So two builds against the same
          # source tree legitimately produce two different
          # _integrity_signature.py files — same INTEGRITY_DIGEST_HEX
          # (computed by _compute_module_digest over the OTHER .py
          # files, which IS what audit Issue 10 / INVARIANT-8 require
          # be reproducible), but different (pubkey, signature).
          #
          # The check below therefore:
          #   1. STRICT — INTEGRITY_DIGEST_HEX must match across both
          #      builds (the artefact whose stability the import-time
          #      gate depends on).
          #   2. STRICT — every .py file EXCEPT _integrity_signature.py
          #      must be byte-identical across both builds (these are
          #      the files the digest is computed over; if any of
          #      them drifts, the digest above would also drift).
          #   3. EXPECTED-TO-DIFFER — _integrity_signature.py itself.
          #      Reported as advisory in the log; not a fail trigger.
          python - <<'PY'
          import hashlib
          import re
          import sys
          import zipfile
          from pathlib import Path

          INTEGRITY_SIG_BASENAME = "_integrity_signature.py"

          def load_wheel(name: str):
              wheels = sorted(Path(name).glob("*.whl"))
              if not wheels:
                  sys.exit(f"FAIL: no wheel in {name}/")
              return wheels[0]

          def extract_py_and_digest(wheel: Path):
              py_contents = {}
              digest_hex = None
              with zipfile.ZipFile(wheel) as zf:
                  for info in zf.infolist():
                      if not info.filename.endswith(".py"):
                          continue
                      data = zf.read(info.filename)
                      py_contents[info.filename] = data
                      if info.filename.endswith(INTEGRITY_SIG_BASENAME):
                          m = re.search(
                              r'INTEGRITY_DIGEST_HEX\s*=\s*"([0-9a-fA-F]+)"',
                              data.decode("utf-8"),
                          )
                          if m:
                              digest_hex = m.group(1)
              if digest_hex is None:
                  sys.exit(f"FAIL: no INTEGRITY_DIGEST_HEX in {wheel}")
              return py_contents, digest_hex

          py1, d1 = extract_py_and_digest(load_wheel("dist1"))
          py2, d2 = extract_py_and_digest(load_wheel("dist2"))

          # (1) Strict — the digest itself must match.  This is what
          #     audit Issue 10 / INVARIANT-8 ultimately gate on.
          print(f"Pass 1 INTEGRITY_DIGEST_HEX: {d1}")
          print(f"Pass 2 INTEGRITY_DIGEST_HEX: {d2}")
          if d1 != d2:
              sys.exit(
                  "FAIL (INVARIANT-8): INTEGRITY_DIGEST_HEX differs between "
                  "two builds with identical inputs.  The wheel-integrity "
                  "gate at import time becomes a coin flip across "
                  "redeploys.  Investigate the .py file set, environment, "
                  "and timestamps."
              )

          # (2) Strict — the file list must agree on every .py file
          #     EXCEPT _integrity_signature.py.  The signature file is
          #     generated by both builds (no asymmetric presence), but
          #     we tolerate "present in both" / "missing in both" only
          #     after stripping it from the diff.
          def strip_sig(names):
              return {n for n in names if not n.endswith(INTEGRITY_SIG_BASENAME)}

          set1 = strip_sig(py1)
          set2 = strip_sig(py2)
          if set1 != set2:
              only_1 = sorted(set1 - set2)
              only_2 = sorted(set2 - set1)
              sys.exit(
                  f"FAIL: wheel .py file lists differ (ignoring signature).\n"
                  f"  Only in pass 1: {only_1}\n"
                  f"  Only in pass 2: {only_2}"
              )

          # (3) Strict — every .py file other than the signature must
          #     match byte-for-byte.  The signature is in the same
          #     wheel but is EXPECTED to differ; we report its
          #     divergence at the bottom as advisory only.
          mismatches = []
          for name in sorted(set1):
              if py1[name] != py2[name]:
                  h1 = hashlib.sha256(py1[name]).hexdigest()[:16]
                  h2 = hashlib.sha256(py2[name]).hexdigest()[:16]
                  mismatches.append(f"  {name}: pass1={h1}... pass2={h2}...")
          if mismatches:
              sys.exit(
                  "FAIL: per-file .py content differs between builds "
                  "(excluding the ephemeral signature artefact):\n"
                  + "\n".join(mismatches)
              )

          # (4) Advisory — surface the (expected) signature-file divergence
          #     so a reader scanning the log can confirm both wheels did
          #     ship a freshly signed integrity artefact (INVARIANT-17
          #     ephemeral per-build keypair behaviour).
          sig1_name = next(
              (n for n in py1 if n.endswith(INTEGRITY_SIG_BASENAME)), None
          )
          sig2_name = next(
              (n for n in py2 if n.endswith(INTEGRITY_SIG_BASENAME)), None
          )
          if sig1_name and sig2_name:
              same = py1[sig1_name] == py2[sig2_name]
              print(
                  f"INFO: {INTEGRITY_SIG_BASENAME} byte-equal across builds: "
                  f"{same} (False is expected — see INVARIANT-17)."
              )

          print(f"OK: INTEGRITY_DIGEST_HEX {d1} stable across both builds.")
          print(
              f"OK: all {len(set1)} non-signature .py files inside the "
              "wheel are byte-identical."
          )
          PY

      - name: Diff native artefacts (STRICT)
        # Audit Issue 10 close-out: promoted from ADVISORY to STRICT.
        # `.so` / `.pyd` / Cython-built native artefacts must now be
        # byte-identical across both passes.  This is the affirmative
        # side of INVARIANT-8 (deterministic reproducible builds).
        #
        # Exclusions:
        #   *.py       — covered by the digest + .py byte-equality
        #                check above, including the legitimate
        #                INVARIANT-17 exemption for the per-build
        #                ephemeral `_integrity_signature.py`.  Re-diffing
        #                them here would tautologically fail on that one
        #                file.
        #   RECORD     — pip regenerates the wheel `RECORD` manifest
        #                per build; the manifest's hashes already cover
        #                every other artefact, so its byte-equality
        #                follows transitively from the rest.
        #
        # No `continue-on-error`, no `|| true`: a divergence here is a
        # hard fail.  When a previously-non-deterministic artefact
        # becomes deterministic, tighten the exclude list — do NOT
        # add a new per-path exemption to absorb a regression.
        #
        # On failure, emits per-file diagnostic info (size delta,
        # first-divergence offset via `cmp -l`, and `objdump -h`
        # section headers when the differing file is an ELF object)
        # so a maintainer reading the CI log can identify the
        # category of non-determinism (build-id, DWARF, sections,
        # .note metadata, etc.) without having to download the wheel
        # artefact and diff locally.
        run: |
          set -e
          mkdir -p /tmp/w1 /tmp/w2
          (cd /tmp/w1 && unzip -q "$GITHUB_WORKSPACE"/dist1/*.whl)
          (cd /tmp/w2 && unzip -q "$GITHUB_WORKSPACE"/dist2/*.whl)
          echo "=== Native artefact diff (strict) ==="
          # Capture --brief output without exiting; the diagnostic
          # block below depends on us reaching it.
          diff_rc=0
          differ=$(diff --brief --recursive --exclude='*.py' --exclude=RECORD \
            /tmp/w1 /tmp/w2 || true)
          if [ -z "$differ" ]; then
            echo "OK: wheel native artefacts byte-equal across both builds."
            exit 0
          fi
          echo "$differ"
          echo
          echo "=== Per-file divergence diagnostic ==="
          # Parse `diff --brief`'s "Files X and Y differ" lines and
          # emit size delta + first-divergence offset + (for ELF)
          # section-header diff for each.  Helps identify whether the
          # delta is in .note.gnu.build-id, .debug_info, .text, .data,
          # or somewhere else.
          echo "$differ" | while IFS= read -r line; do
            f1=$(echo "$line" | awk '{print $2}')
            f2=$(echo "$line" | awk '{print $4}')
            [ -f "$f1" ] && [ -f "$f2" ] || continue
            sz1=$(stat -c '%s' "$f1")
            sz2=$(stat -c '%s' "$f2")
            echo "---"
            echo "File: $(basename "$f1")"
            echo "  size pass1=$sz1  pass2=$sz2  delta=$((sz2 - sz1))"
            # First-divergence offset via cmp.
            off=$(cmp "$f1" "$f2" 2>&1 | head -1 || true)
            echo "  $off"
            # If the file is an ELF object, dump and diff section
            # header sizes so the maintainer can see which section
            # carries the delta.
            if file "$f1" 2>/dev/null | grep -q ELF; then
              echo "  ELF section-header diff (size column):"
              diff <(objdump -h "$f1" | awk '/^ *[0-9]+ /{print $2,$3}') \
                   <(objdump -h "$f2" | awk '/^ *[0-9]+ /{print $2,$3}') | head -20 || true
            fi
          done
          echo
          echo "::error::Reproducible-build native-artefact diff failed — see per-file diagnostic above."
          exit 1

      - name: Upload both wheels for offline diff
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: reproducible-build-wheels
          path: |
            dist1/*.whl
            dist2/*.whl
          retention-days: 14

  memory-sanitizer-kat:
    name: MemorySanitizer (fast KAT lane, per-PR)
    # Fast per-PR MSan lane: build ONLY the OpenSSL-free `test_kat` target under
    # MemorySanitizer and run it. Feasible on every PR because test_kat drives an
    # AES-256-CTR DRBG entirely through AMA's own constant-time AES (no
    # third-party crypto), so the whole binary is MSan-instrumented. It mirrors
    # the nightly `memory-sanitizer` job's flags EXACTLY (clang-18, libc++,
    # -fsanitize-memory-track-origins=2, SIMD off), so a green nightly implies a
    # green fast lane and vice-versa. No schedule `if:` guard — this one runs on
    # every push/PR; the heavier full-suite MSan/TSan/Valgrind stay nightly-only.
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      # This step is where the apt-hang problem was first diagnosed (868c354),
      # and it carried the only copy of the fix.  The retry policy now lives
      # in .github/scripts/apt-install.sh so every apt call in every workflow
      # gets it — the three jobs cancelled on a later push were three steps
      # that this inline version could not reach.  The rationale it used to
      # carry is in that script's header.
      - name: Install dependencies
        run: |
          .github/scripts/apt-install.sh cmake clang-18 libc++-18-dev libc++abi-18-dev

      - name: Configure with MSan (KAT-only, SIMD off, const-time AES)
        run: |
          cmake -B build-msan-kat \
            -DCMAKE_C_COMPILER=clang-18 \
            -DCMAKE_CXX_COMPILER=clang++-18 \
            -DCMAKE_C_FLAGS="-fsanitize=memory -fsanitize-memory-track-origins=2 -fno-omit-frame-pointer -g -O1" \
            -DCMAKE_CXX_FLAGS="-fsanitize=memory -fsanitize-memory-track-origins=2 -fno-omit-frame-pointer -g -O1 -stdlib=libc++" \
            -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=memory -stdlib=libc++" \
            -DAMA_USE_NATIVE_PQC=ON \
            -DAMA_BUILD_TESTS=ON \
            -DAMA_BUILD_EXAMPLES=OFF \
            -DAMA_ENABLE_LTO=OFF \
            -DAMA_ENABLE_SIMD=OFF \
            -DAMA_AES_CONSTTIME=ON

      - name: Build only the test_kat target (+ its deps)
        run: cmake --build build-msan-kat --target test_kat -j"$(nproc)"

      - name: Run test_kat under MSan
        env:
          MSAN_OPTIONS: print_stacktrace=1:halt_on_error=1:abort_on_error=1
        # `ctest -R` honours the add_test WORKING_DIRECTORY (repo root) needed to
        # locate the NIST KAT vector files. Anchored regex matches exactly test_kat.
        run: ctest --test-dir build-msan-kat -R '^test_kat$' --output-on-failure

  static-analysis-gate:
    name: Static Analysis Gate
    # Single aggregating status check for this workflow (see the matching gate
    # in ci.yml). `needs:` is workflow-local. It lists EVERY job, including the
    # schedule/dispatch-only sanitizer deep-lanes: a genuine MSAN/TSAN/Valgrind
    # *failure* on a scheduled run must turn this gate red, and it did not while
    # they were omitted.
    if: always()
    needs:
      - cppcheck
      - clang-analyzer
      - codeql
      - compiler-warnings
      - compiler-warnings-aarch64
      - version-consistency
      - address-sanitizer
      - memory-sanitizer
      - thread-sanitizer
      - valgrind-memcheck
      - clang-tidy
      - reproducible-build
      - memory-sanitizer-kat
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Fail unless every job reached the state this trigger requires
        # A gate that demanded `success` from all thirteen jobs would be red on
        # every pull request, because memory-sanitizer / thread-sanitizer /
        # valgrind-memcheck are schedule/dispatch-only and skip on PRs by
        # design, and clang-tidy / reproducible-build skip on a non-PR push. A
        # gate that tolerates `skipped` unconditionally (the previous behaviour)
        # has the opposite problem: a job that silently stopped running reports
        # green.
        #
        # So, like dudect-gate, this re-derives each job's own trigger and
        # asserts the state that trigger implies:
        #   should run  -> result MUST be `success`  (a skip is a silent gap)
        #   should skip -> result MUST be `skipped`  (a run is `if:` drift)
        #
        # The conditions below MUST mirror the job-level `if:` on each job:
        #   sanitizer deep-lanes : schedule || workflow_dispatch
        #   clang-tidy / repro   : schedule || workflow_dispatch || pull_request
        #   everything else      : unconditional
        # GitHub Actions cannot reference another job's condition, so this
        # duplication is the cost of checking it at all.
        env:
          EVENT: ${{ github.event_name }}
          R_CPPCHECK: ${{ needs.cppcheck.result }}
          R_CLANG_ANALYZER: ${{ needs.clang-analyzer.result }}
          R_CODEQL: ${{ needs.codeql.result }}
          R_WARNINGS: ${{ needs.compiler-warnings.result }}
          R_WARNINGS_ARM: ${{ needs.compiler-warnings-aarch64.result }}
          R_VERSION: ${{ needs.version-consistency.result }}
          R_ASAN: ${{ needs.address-sanitizer.result }}
          R_MSAN: ${{ needs.memory-sanitizer.result }}
          R_TSAN: ${{ needs.thread-sanitizer.result }}
          R_VALGRIND: ${{ needs.valgrind-memcheck.result }}
          R_CLANG_TIDY: ${{ needs.clang-tidy.result }}
          R_REPRO: ${{ needs.reproducible-build.result }}
          R_MSAN_KAT: ${{ needs.memory-sanitizer-kat.result }}
        run: |
          set -euo pipefail
          rc=0

          # Sanitizer deep-lanes: schedule or manual dispatch only.
          if [ "${EVENT}" = "schedule" ] || [ "${EVENT}" = "workflow_dispatch" ]; then
            sanitizer_expected="success"
          else
            sanitizer_expected="skipped"
          fi
          # clang-tidy + reproducible-build: also on pull_request.
          if [ "${EVENT}" = "schedule" ] || [ "${EVENT}" = "workflow_dispatch" ] \
             || [ "${EVENT}" = "pull_request" ]; then
            prlane_expected="success"
          else
            prlane_expected="skipped"
          fi

          check() {
            job="$1"; actual="$2"; expected="$3"
            if [ "${actual}" = "${expected}" ]; then
              echo "  OK    ${job}: ${actual} (expected ${expected})"
              return 0
            fi
            if [ "${expected}" = "success" ] && [ "${actual}" = "skipped" ]; then
              echo "::error::${job} was SKIPPED but this trigger requires it to run."
            elif [ "${expected}" = "skipped" ]; then
              echo "::error::${job} ran (${actual}) but this trigger should skip it — its \`if:\` condition drifted."
            else
              echo "::error::${job} did not succeed: ${actual}"
            fi
            rc=1
          }

          echo "event=${EVENT}"
          echo "expected: sanitizer deep-lanes=${sanitizer_expected}, clang-tidy/repro=${prlane_expected}"
          check cppcheck             "${R_CPPCHECK}"       success
          check clang-analyzer       "${R_CLANG_ANALYZER}" success
          check codeql               "${R_CODEQL}"         success
          check compiler-warnings    "${R_WARNINGS}"       success
          check compiler-warnings-aarch64 "${R_WARNINGS_ARM}" success
          check version-consistency  "${R_VERSION}"        success
          check address-sanitizer    "${R_ASAN}"           success
          check memory-sanitizer-kat "${R_MSAN_KAT}"       success
          check memory-sanitizer     "${R_MSAN}"           "${sanitizer_expected}"
          check thread-sanitizer     "${R_TSAN}"           "${sanitizer_expected}"
          check valgrind-memcheck    "${R_VALGRIND}"       "${sanitizer_expected}"
          check clang-tidy           "${R_CLANG_TIDY}"     "${prlane_expected}"
          check reproducible-build   "${R_REPRO}"          "${prlane_expected}"

          if [ "${rc}" -ne 0 ]; then
            echo "::error::Static Analysis Gate failed."
            exit 1
          fi
          echo "Static Analysis Gate passed — every job reached the state this trigger requires."
